# EpiSODIC, open items

Gaps and parked decisions not yet resolved in the codebase.

- **`episodic_stream`'s `ward` column** is a schema addition beyond the
  original design — flagged for review rather than treated as settled.
- **Cluster volume for endemic organisms at a single place** — two
  directions were discussed and deliberately parked.
- **Calibration of weights and thresholds against real signal** — the
  tools exist (Prestatie screen, synthetic calibration generator); the
  calibration itself needs a real instance with real history.
- **Mute revocation** and **region-reference naming beyond the
  choropleth's own geometry contract** are documented, narrow gaps, not
  yet built.
