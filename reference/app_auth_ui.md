# Authentication UI

"The app opens read-only for anyone who reaches it... login only to
classify". A small header control and two modals (sign in; forced
first-login password change) are the entire authentication surface - no
separate screen, no account management UI (four accounts, provisioned
outside the app).
