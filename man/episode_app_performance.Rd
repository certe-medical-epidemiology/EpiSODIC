% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app_performance.R
\name{episode_app_performance}
\alias{episode_app_performance}
\title{The Performance screen: positive predictive value, timeliness, classification mix}
\usage{
episode_app_performance(con, lang = "nl")
}
\arguments{
\item{con}{A \link[DBI:DBIConnection-class]{DBI::DBIConnection}.}

\item{lang}{Session language, for verdict labels.}
}
\value{
A list with \code{by_detector_pathogen} (a data frame: \code{detector},
\code{pathogen}, \code{n_detections}, \code{n_true_positive}, \code{n_false_positive},
\code{ppv}), \code{classification_distribution} (a data frame: \code{verdict},
\code{verdict_label}, \code{n}), and \code{timeliness} (a list of three
\code{list(median_days, n)} entries: \code{to_detection},
\code{to_first_assessment}, \code{to_classification} - \code{median_days} is \code{NA}
when \code{n} is \code{0}).
}
\description{
ARCHITECTURE.md section 9 ("Prestatie"): "Positive predictive value
per detector per organism computed from the stored verdicts, time from
first case to detection, and time from detection to first assessment.
This is how tuning decisions get justified, how the eligibility gate
gets calibrated towards the target of roughly ten clusters a month,
and it is publishable in its own right." Timeliness is read from the
\code{episode_cluster}/\code{episode_assessment_event} timestamps directly
(\code{opened_at}, \code{first_day}, each cluster's own first assessment/
classification event) rather than reconstructed from the state
trajectory - section 6.4's "supplies the timeliness figures on the
Prestatie screen directly" is about state \emph{transitions}, and the
timestamps this screen needs (first case, detection, first
assessment, first classification) are exactly the ones already
recorded on \code{episode_cluster} and \code{episode_assessment_event} without
needing the state trajectory at all.
}
\details{
A terminal verdict decides "positive predictive": \code{possible_epidemic}
and \code{confirmed_epidemic} count as a true positive for every detector
that ever flagged the cluster; \code{artefact} and \code{expected_variation}
count as a false positive. \code{cluster_not_yet} (still undecided) and an
unassessed cluster are excluded from PPV entirely - they are not yet
a judgement either way, not a form of "wrong".

This is a cheap read (ARCHITECTURE.md section 3.3), same as the rest
of \code{R/app_read.R}; the aggregation happens in R over already-fetched
rows, not in SQL, since SQLite's own \verb{GROUP BY} gains nothing here at
the volumes this system runs at (ARCHITECTURE.md section 3.3 on
choosing SQLite in the first place).
}
