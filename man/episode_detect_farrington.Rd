% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detect_farrington.R
\name{episode_detect_farrington}
\alias{episode_detect_farrington}
\title{Farrington detector}
\usage{
episode_detect_farrington(
  cases_for_stream,
  stream_id,
  config,
  run_date = Sys.Date(),
  population = NULL
)
}
\arguments{
\item{cases_for_stream}{A data frame of a single stream's cases, with
\code{sample_date}.}

\item{stream_id}{The stream these cases belong to.}

\item{config}{The resolved configuration; uses \code{config$farrington}.}

\item{run_date}{The date to treat as "today"; the most recent week
evaluated is the one containing this date.}

\item{population}{An optional numeric vector of weekly population
(patient-days) aligned to \code{episode_weekly_bins()}'s own weeks, from
\code{\link[=episode_farrington_population_vector]{episode_farrington_population_vector()}}. \code{NULL} (default) fits on
raw counts, unnormalised - the only behaviour possible before
ARCHITECTURE.md section 7.1's patient-day normalisation existed, and
still what every stream without institution activity data gets.}
}
\value{
A data frame of detection records (zero or one row: Farrington
evaluates only the current week, per ARCHITECTURE.md section 7).
}
\description{
Wraps \code{surveillance::farringtonFlexible()} directly (CRAN, \code{Imports}, no
Certe dependency), replacing the earlier \code{certestats::detect_farrington()}
wrapper. \code{certestats} was only ever a thin linelist-to-\code{sts} adapter
around this exact function; owning that glue removes the last
Certe-specific package from the detection path and, unlike the
\code{certestats} wrapper it replaces, can actually be installed and tested in
any environment, CI included.
}
\details{
Case dates are aggregated into weekly bins (Monday-starting), which is
standard practice for Farrington-style surveillance; \code{frequency = 52} is
used for the underlying \code{sts} object, the conventional approximation
(a true year is ~52.18 weeks, so bin-to-calendar alignment drifts
slightly over many years - a documented, widely-accepted simplification,
not an EpiSODE-specific one). Detection is only evaluated for the most
recent complete week relative to \code{run_date}.
}
