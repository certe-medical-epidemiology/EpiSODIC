% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auth.R
\name{auth}
\alias{auth}
\title{Authentication}
\description{
A handful of accounts, hashed with \code{sodium::password_store()} and checked
with \code{sodium::password_verify()}. No
lockout, no backoff, and no TLS is implemented \emph{by this package}: the
login exists to attribute assessments, not to defend against attackers,
and network-level access control (VPN, internal network, a reverse
proxy terminating TLS) is the deploying operator's own responsibility,
not something this package provides or assumes. Since a single R
process serves every session,
\code{Sys.info()[["user"]]} returns the host account rather than the
assessor, so the login is the only available identity source.
}
\details{
Password changes and login timestamps are the one bit of per-user
\emph{mutable} state in the schema, yet the app never issues an \code{UPDATE}.
Resolved the same way \verb{episodic_cluster_ state} already resolves it for cluster state: \code{episodic_app_user_event}
is an append-only log, and the "current" password hash / login time is
derived from it at read time (see \code{episodic_auth_password_hash()},
\code{episodic_auth_last_login()}), falling back to \code{episodic_app_user}'s own
initial values when no event has been recorded yet.
}
