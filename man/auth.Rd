% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auth.R
\name{auth}
\alias{auth}
\title{Authentication}
\description{
Four accounts, hashed with \code{sodium::password_store()} and checked with
\code{sodium::password_verify()} (ARCHITECTURE.md section 12). No lockout, no
backoff, no TLS requirement: access already requires being on a Certe
thin client. The login exists to attribute assessments, not to defend
against attackers - since a single R process serves every session,
\code{Sys.info()[["user"]]} returns the host account rather than the
assessor, so the login is the only available identity source.
}
\details{
Password changes and login timestamps are the one bit of per-user
\emph{mutable} state in the schema, yet the app never issues an \code{UPDATE}
(standing brief hard rule 7). Resolved the same way \verb{episode_cluster_ state} already resolves it for cluster state: \code{episode_app_user_event}
is an append-only log, and the "current" password hash / login time is
derived from it at read time (see \code{episode_auth_password_hash()},
\code{episode_auth_last_login()}), falling back to \code{episode_app_user}'s own
initial values when no event has been recorded yet.
}
