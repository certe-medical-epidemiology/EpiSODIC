% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detect_farrington.R
\name{episode_farrington_population_vector}
\alias{episode_farrington_population_vector}
\title{Build a weekly patient-days vector aligned to Farrington's weekly bins}
\usage{
episode_farrington_population_vector(con, institution_id, level, week_start)
}
\arguments{
\item{con}{A \link[DBI:DBIConnection-class]{DBI::DBIConnection}.}

\item{institution_id}{An \code{episode_institution} id, or \code{NA}.}

\item{level}{A stream's \code{level}, from \code{episode_stream}.}

\item{week_start}{The \code{Date} vector from \code{episode_weekly_bins()}.}
}
\value{
A numeric vector the same length as \code{week_start}, or \code{NULL}.
}
\description{
ARCHITECTURE.md section 7.1: institution streams (L2) should use
\code{farringtonFlexible()}'s \code{populationOffset} with weekly patient-days
rather than raw counts, since occupancy varies seasonally and a
count-based baseline would alarm on a busy February and stay silent
through a quiet August at identical transmission-per-patient-day. \code{NULL}
when \code{institution_id} is \code{NA} (non-institution streams) or no activity
data exists at all for it - callers pass \code{NULL} straight through to
\code{\link[=episode_detect_farrington]{episode_detect_farrington()}}/\code{\link[=episode_farrington_trend]{episode_farrington_trend()}}, which then
fit on raw counts exactly as before this existed.
}
\details{
Only \code{episode_institution_activity} (L2, whole-institution) exists in
the schema - there is no ward-level (L1) activity table, so L1 streams
always get \code{NULL} here regardless of \code{institution_id}; ARCHITECTURE.md
section 7 itself only promises ward patient-days "if obtainable", and
they are not (see QUESTIONS.md).
}
