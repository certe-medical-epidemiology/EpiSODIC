% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geo_data.R
\name{episodic_geo}
\alias{episodic_geo}
\alias{episodic_geo_overlay_resolve}
\alias{episodic_geo_source_resolve}
\title{Show Clusters on a Map}
\usage{
episodic_geo_overlay_resolve(
  path = Sys.getenv("EPISODIC_GEO_DATA_OVERLAY", unset = NA)
)

episodic_geo_source_resolve(path = Sys.getenv("EPISODIC_GEO_DATA", unset = NA))
}
\arguments{
\item{path}{Path to an \code{.rds} file holding an \code{sf} object with \code{pc}
and \code{geometry} columns. Defaults to the \code{EPISODIC_GEO_DATA}
environment variable. With none set, there is no map and the
dashboard shows its bar-chart fallback instead.}
}
\value{
An \code{sf} object, or \code{NULL} if \code{sf} is not installed, no path is
set, or the file is missing or invalid.
}
\description{
The dashboard can plot cluster case counts on a choropleth map by postcode
(or any other geographic unit you use), provided the optional \code{sf}
package is installed and geographic reference data is available. Without
either, the dashboard still works fine - it just falls back to a bar
chart of case counts by area instead of a map.
}
\details{
EpiSODIC ships with Dutch four-digit postcode geometry as a working
default, but is not tied to the Netherlands or to postcodes: point the
\code{EPISODIC_GEO_DATA} environment variable at your own \code{.rds} file (an
\code{sf} object with a \code{pc} column matching your case data's area codes, and
a \code{geometry} column) to map your own region instead.

You can optionally add a second, purely visual layer of outlines - e.g.
province or municipality borders - drawn on top of the choropleth for
orientation, via \code{EPISODIC_GEO_DATA_OVERLAY} and
\code{\link[=episodic_geo_overlay_resolve]{episodic_geo_overlay_resolve()}}. This layer carries no case counts, so
it only needs a \code{geometry} column.
}
\section{Why there is no default map}{

A default map is a default \emph{country}. Falling back to the Netherlands
PC4 geometry bundled with the package would give a laboratory
anywhere else a silent, confident map of the Netherlands with its own
case counts joined onto whichever Dutch postcodes happened to share a
number - postcode-like codes are four digits in a great many
countries - and a plausible-looking wrong answer is worse than no
answer. So there is no default: with nothing configured the map is
simply absent, and the dashboard falls back to a bar chart of case
counts by area, which is correct everywhere.

\code{episodic_demo()} sets \code{EPISODIC_GEO_DATA} to the bundled Netherlands
geometry itself, so the demo still shows a map.
}

\examples{
# NULL when unset (or when the sf package is not installed)
episodic_geo_overlay_resolve(path = NA)
# NULL when unset (or when the sf package is not installed)
episodic_geo_source_resolve(path = NA)
}
