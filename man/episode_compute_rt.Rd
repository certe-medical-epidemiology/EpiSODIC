% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analytics_rt.R
\name{episode_compute_rt}
\alias{episode_compute_rt}
\title{Time-varying effective reproduction number (Rt) for a cluster}
\usage{
episode_compute_rt(cases, pc, incomplete_days = 0L, window_days = 7L)
}
\arguments{
\item{cases}{A data frame of the cluster's cases, with \code{sample_date}.}

\item{pc}{A single-row pathogen config (\code{episode_db_pathogen_config_get()}).}

\item{incomplete_days}{From \code{episode_app_completeness()}; days at the
trailing end of the case series considered under-ascertained.}

\item{window_days}{Sliding window width, days. Fixed at 7 (weekly),
matching every other panel's own aggregation.}
}
\value{
A data frame with \code{window_end} (\code{Date}), \code{mean}, \code{lower},
\code{upper} (2.5\%/97.5\% credible interval), or \code{NULL} if Rt cannot or
should not be computed.
}
\description{
\code{EpiEstim::estimate_R()} on sample-date incidence, sliding 7-day
windows, using the pathogen's serial interval from
\code{episode_pathogen_config} (ARCHITECTURE.md section 9). This is Rt, the
time-varying \emph{effective} reproduction number - not R0 - and any display
of it must say so.
}
\details{
Suppressed entirely (returns \code{NULL}) where \code{rt_applicable} is \code{FALSE},
or the serial interval is unset, or there is not enough case history
for even one window: no off-the-shelf message like "insufficient data"
is a substitute for simply not showing the panel, per how the rest of
this codebase treats optional analytical panels (\verb{episode_cluster_ object()}'s other \code{NULL}-when-not-computable fields).

Estimates whose window ends inside \code{incomplete_days} of the run date
are withheld, not shown and captioned - "estimates within the
truncated window withheld rather than captioned" (ARCHITECTURE.md
section 9). The reporting delay means the most recent days are
under-ascertained by construction (\code{R/triangle_update.R}), so an Rt
estimate ending there would read a reporting artefact as a real change
in transmission.

\code{si_dist} (\code{gamma}/\code{lognormal}) from \code{episode_pathogen_config} is
recorded but not yet used to pick \code{EpiEstim}'s distribution family:
\code{estimate_R(method = "parametric_si")} only ever fits a Gamma-shaped
serial interval internally (mean/sd parameterised, \code{EpiEstim}'s own
standard usage), regardless of what a pathogen's own literature source
assumed. This is a known simplification, not a silent one - see
QUESTIONS.md.
}
