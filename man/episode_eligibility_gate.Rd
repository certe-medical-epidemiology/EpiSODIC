% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lattice_eligibility.R
\name{episode_eligibility_gate}
\alias{episode_eligibility_gate}
\title{Eligibility gate}
\usage{
episode_eligibility_gate(cases_for_stream, as_of, config)
}
\arguments{
\item{cases_for_stream}{A data frame of cases belonging to one stream,
with a \code{sample_date} column.}

\item{as_of}{The date detection is being run as of; baseline weeks are
counted back from here.}

\item{config}{The resolved configuration (\code{episode_config_resolve()});
uses \code{config$eligibility}.}
}
\value{
\code{TRUE} if the stream is eligible for statistical detection,
\code{FALSE} otherwise.
}
\description{
The first layer of volume control (ARCHITECTURE.md section 8, item 1): a
stream needs sufficient baseline history, a minimum median weekly count,
and non-zero counts in a reasonable share of baseline weeks before
statistical detection is attempted on it. Streams failing this gate are
documented in \code{MILESTONES.md}/the architecture as falling through to
EARS C2 or the rare-pathogen path; only the gate itself is implemented in
M1 (\code{R/detect_*.R} handles the fall-through detectors).
}
\details{
The numeric thresholds are not specified anywhere in the architecture
beyond the three named criteria; see \code{QUESTIONS.md} item 16 for the
provisional defaults adopted and shipped in \code{inst/config/default.yaml}.
}
