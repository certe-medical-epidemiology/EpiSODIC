% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cases_check.R
\name{episodic_check_cases}
\alias{episodic_check_cases}
\title{Check your case data before you hand it to EpiSODIC}
\usage{
episodic_check_cases(cases, stop_on_problem = FALSE)
}
\arguments{
\item{cases}{Your case data: a data frame or \code{tibble} in the shape
\link{episodic_case_data} describes, or a zero-argument function returning
one. Anything else is itself reported as a problem rather
than throwing - the point of this function is that it always answers.}

\item{stop_on_problem}{A \link{logical} to indicate whether an error must be
thrown if any problem is found. Default is \code{FALSE}.}
}
\value{
A data frame of findings with class \code{episodic_case_check} and
one row per finding, with columns \code{severity} (\code{"problem"} or
\code{"advice"}), \code{issue}, \code{column}, \code{n_rows}, \code{rows}, \code{values},
\code{message} and \code{fix}. Zero rows means the data set passed every check.
A summary of what was read (rows, columns, date range, counts) is
attached as the \code{"summary"} attribute and shown when printing.
}
\description{
Runs every check the \link{episodic_case_data} contract implies over your
extract and reports \emph{everything} it finds in one go - what is wrong,
how many rows are affected, which rows those are, what the offending
values look like, and what to do about each one. Nothing is written,
nothing is changed, and no database is needed: this is the call to make
while you are still building your extract step, and the first call to
make when a run did not produce what you expected.
}
\details{
Use \code{episodic_check_cases()} plainly when you want to \emph{look}, or
\code{episodic_check_cases(..., stop_on_problem = TRUE)} when you want a script
to stop. The latter is what \code{\link[=episodic_run_cron]{episodic_run_cron()}} itself calls before a run.
}
\section{What it reports}{


Two kinds of finding, deliberately kept apart:

\describe{
\item{Problems}{Things EpiSODIC cannot work around: a missing or
unexpected column, an empty value in a column that must be filled,
a value outside a fixed set, a date that does not read as a date, a
duplicated \code{source_key}. A run refuses to start while any of these
stand.}
\item{Advice}{Things that are \emph{allowed} but rarely intended, and that
quietly cost you signal: no \code{ward} on any hospital row (so
ward-level detection has nothing to group on), one pathogen spelled
two ways (so its cases are split over two streams), a
\code{patient_key} that is unique per row (so deduplication and episode
grouping cannot do anything), postcodes that the geography panel
cannot place, sample dates in the future. A run proceeds
regardless; these are for you to judge.}
}

The value prints as a report; it is also a plain data frame, one row
per finding, so you can work with it programmatically (see the
examples).
}

\examples{
cases <- episodic_synthetic_cases(
  start_date = as.Date("2025-01-01"), end_date = as.Date("2025-01-31")
)
episodic_check_cases(cases)

# a typical extract mistake: dates written day-first, sex as words
broken <- cases
broken$sample_date <- format(as.Date(broken$sample_date), "\%d-\%m-\%Y")
broken$sex <- ifelse(broken$sex == "M", "male", "female")
report <- episodic_check_cases(broken)
report

# it is a data frame too
report$column[report$severity == "problem"]
}
\seealso{
\link{episodic_case_data} for what each column means and which
values it accepts.
}
