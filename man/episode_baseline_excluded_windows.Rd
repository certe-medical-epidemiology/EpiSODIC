% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/baseline_feedback.R
\name{episode_baseline_excluded_windows}
\alias{episode_baseline_excluded_windows}
\title{Baseline feedback: exclude confirmed-epidemic periods from detection}
\usage{
episode_baseline_excluded_windows(con, stream_id)
}
\arguments{
\item{con}{A \link[DBI:DBIConnection-class]{DBI::DBIConnection}.}

\item{stream_id}{A stream id.}
}
\value{
A data frame with \code{first_day}, \code{last_day} (character, ISO
dates), one row per cluster in this stream whose latest verdict is
\code{confirmed_epidemic}. Zero rows if none.
}
\description{
ARCHITECTURE.md section 7.6: periods classified as a confirmed epidemic
are excluded from the baseline of subsequent Farrington runs for that
stream, derived from \code{episode_assessment_event} so it updates
automatically when a verdict is revised. \code{farringtonFlexible()}
downweights past aberrations statistically, but a human verdict is
better evidence than a residual - without this exclusion, last winter's
outbreak silently raises this winter's threshold.
}
