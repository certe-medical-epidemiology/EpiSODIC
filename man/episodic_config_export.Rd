% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config_export.R
\name{episodic_config_export}
\alias{episodic_config_export}
\title{Export the resolved configuration as a zip file}
\usage{
episodic_config_export(
  db_path = Sys.getenv("EPISODIC_DB", unset = NA),
  episodic_config_path = Sys.getenv("EPISODIC_CONFIG", unset = NA),
  output_dir = NULL,
  include_secrets = FALSE
)
}
\arguments{
\item{db_path}{Path to the EpiSODIC database: an existing SQLite file,
or a MariaDB/MySQL DSN (see \code{\link[=episodic_db_dsn_mariadb]{episodic_db_dsn_mariadb()}}). Defaults to
the \code{EPISODIC_DB} environment variable. Only used to read a
Settings-screen \code{notifications} override, if one exists - \code{NA}/unset
skips this and exports the YAML-resolved configuration only.}

\item{episodic_config_path}{Passed to \code{\link[=episodic_config_resolve]{episodic_config_resolve()}}.}

\item{output_dir}{Directory to write the zip into. Defaults to a
\verb{config_exports/} directory next to \code{db_path} (mirroring where
\code{\link[=episodic_report_render]{episodic_report_render()}} writes outbreak reports), or a temporary
directory if \code{db_path} is not set.}

\item{include_secrets}{If \code{FALSE} (the default), notification secrets
(SMTP/webhook/client passwords) are replaced with \code{"***"} in the
exported configuration - the same masking the Settings screen applies
on-screen. Pass \code{TRUE} only when you specifically intend the export to
be usable to restore working notification channels elsewhere, and can
handle the file with the same care as the secrets themselves.}
}
\value{
Invisibly, the path to the written zip file.
}
\description{
Bundles the fully-resolved configuration (shipped defaults, your
\code{EPISODIC_CONFIG} YAML overlay, and - if \code{db_path} points at a
database - any Settings-screen \code{notifications} override) together with
the pathogen configuration and, if set, the colour palette override,
into a single zip file. This is the Settings screen's "export
configuration" button; it is also just a plain function you can call
from the console to snapshot an instance's configuration for backup or
migration.
}
\examples{
\dontrun{
episodic_config_export(db_path = Sys.getenv("EPISODIC_DB"))
}
}
