% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_rethreshold.R
\name{episodic_validate_rethreshold}
\alias{episodic_validate_rethreshold}
\title{Re-Match a Validation Result at Different Thresholds}
\usage{
episodic_validate_rethreshold(
  result,
  min_recall = result$meta$min_recall,
  min_precision = result$meta$min_precision
)
}
\arguments{
\item{result}{An \code{episodic_validation} object, from
\code{\link[=episodic_validate_detection]{episodic_validate_detection()}} or \code{\link[=episodic_validate_comparator]{episodic_validate_comparator()}}.}

\item{min_recall, min_precision}{The thresholds to apply instead.}
}
\value{
An \code{episodic_validation} object of the same shape, with
\code{meta$min_recall} and \code{meta$min_precision} updated and
\code{meta$rethresholded_from} recording what it was re-matched from.
}
\description{
\code{min_recall} and \code{min_precision} are choices, and a result that only
holds at one particular pair of them is not a result. This applies a
different pair to a replay that has already been run, so a sweep costs
arithmetic rather than another few hours of detection.
}
\details{
It re-derives everything the thresholds touch and nothing else: which
outbreaks count as detected, when, how badly they fragmented, and
which clusters count as true positives. The clusters that were raised,
the runs that raised them and the cases they held are what they were -
those are measurements, and no threshold changes them.
}
\examples{
\donttest{
result <- episodic_validate_detection()
strict <- episodic_validate_rethreshold(result, min_recall = 0.8)
strict$summary[strict$summary$metric == "sensitivity", ]
}
}
\seealso{
\code{\link[=episodic_validate_detection]{episodic_validate_detection()}}
}
