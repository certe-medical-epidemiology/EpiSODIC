% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app_read.R
\name{episode_app_streams_screen}
\alias{episode_app_streams_screen}
\title{Read-only Streams screen data}
\usage{
episode_app_streams_screen(con, page = 1L, page_size = 50L)
}
\arguments{
\item{con}{A \link[DBI:DBIConnection-class]{DBI::DBIConnection}.}

\item{page}{1-based page number.}

\item{page_size}{Streams per page.}
}
\value{
A list with \code{streams} (a data frame, one page's worth),
\code{total} (the total stream count across all pages), \code{page},
\code{page_size}, \code{n_pages}, and \code{config_snapshot} (the parsed JSON from
the latest successful run, or \code{NULL}).
}
\description{
Displays the configuration from the latest run's \code{config_snapshot}, not
from the file (ARCHITECTURE.md section 7.4/10, MILESTONES.md M2).
}
\details{
Paginated, and deliberately at the read-model level rather than only
in the UI: \code{baseline_excluded} is one DB round trip per stream (via
\code{\link[=episode_baseline_excluded_windows]{episode_baseline_excluded_windows()}}, itself one round trip per
cluster in that stream), so computing it for every stream regardless
of what is actually shown made this screen slow to load once a real
instance's stream count grew past a few dozen - the exact bug report
this was written to fix. Slicing to \code{page} before that loop runs means
the cost is bounded by \code{page_size}, not by the total stream count.
}
