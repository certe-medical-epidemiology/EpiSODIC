% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cases_synthetic.R
\name{episodic_synthetic_ground_truth}
\alias{episodic_synthetic_ground_truth}
\title{Read Back What Was Injected Into Synthetic Data}
\usage{
episodic_synthetic_ground_truth(cases)
}
\arguments{
\item{cases}{A data frame from \code{\link[=episodic_synthetic_cases]{episodic_synthetic_cases()}}.}
}
\value{
A list of two data frames:
\describe{
\item{\code{outbreaks}}{One row per injected outbreak: \code{outbreak_id},
\code{label}, \code{pathogen}, \code{institution_key} and \code{ward} (both \code{NA}
where the outbreak was not confined to one), \code{first_day},
\code{peak_day}, \code{last_day}, \code{n_cases}, and the \code{expected_channel}
and \code{expected_level} the shape was designed to exercise.}
\item{\code{cases}}{One row per injected case: \code{outbreak_id} and the
\code{source_key} it carries in the case data, which is what joins it
to \code{episodic_case} once a run has loaded it.}
}
Both are empty (zero rows, same columns) when nothing was injected.
}
\description{
\code{\link[=episodic_synthetic_cases]{episodic_synthetic_cases()}} injects outbreaks into an otherwise
endemic history. This reads back exactly what it injected: one row per
outbreak saying where and when it ran and how large it was, and the
case-level membership that goes with it. It is what makes a detection
result measurable against known truth rather than against an
epidemiologist's verdict on it - see \code{\link[=episodic_validate_detection]{episodic_validate_detection()}}.
}
\details{
Injected cases also carry a recognisable \verb{PT-OUTBREAK-*} patient key,
and it is tempting to recover membership by matching on it. Do not:
that couples whatever does the matching to a naming convention nobody
has undertaken to keep, and it fails silently, as a smaller set of
outbreak cases rather than an error, the first time the convention
changes.
}
\examples{
cases <- episodic_synthetic_cases(
  start_date = as.Date("2025-01-01"), end_date = as.Date("2025-03-31")
)
truth <- episodic_synthetic_ground_truth(cases)
truth$outbreaks[, c("outbreak_id", "pathogen", "n_cases")]
}
\seealso{
\code{\link[=episodic_synthetic_cases]{episodic_synthetic_cases()}}, \code{\link[=episodic_validate_detection]{episodic_validate_detection()}}
}
