% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_comparator.R
\name{episodic_validate_comparator}
\alias{episodic_validate_comparator}
\title{Measure a Trivial Rule Against the Same Known Truth}
\usage{
episodic_validate_comparator(
  method = c("same_place", "shewhart"),
  seeds = 1,
  end_date = Sys.Date(),
  history_years = 1,
  evaluation_weeks = 4,
  min_recall = 0.5,
  min_precision = 0.5,
  outbreaks = TRUE,
  outbreak_offsets = NULL,
  n_cases = 3,
  k_days = 7,
  case_free_days = 14,
  baseline_weeks = 52,
  sd_multiplier = 2,
  quiet = TRUE
)
}
\arguments{
\item{method}{Which rule to run.}

\item{seeds}{RNG seeds to replicate over. Seeds are the replicates:
one realisation of a stochastic generator is an anecdote, so the
summary reports medians and interquartile ranges across them.}

\item{end_date}{The last day of generated history, and the last run
the replay makes. Defaults to today, which means two runs on
different days are not comparable; pin it for anything whose numbers
are going to be quoted.}

\item{history_years}{Years of endemic history before the evaluation
window, for the detectors to build a baseline from.}

\item{evaluation_weeks}{Weekly runs to replay.}

\item{min_recall, min_precision}{The matching thresholds described
above.}

\item{outbreaks, outbreak_offsets}{Passed to
\code{\link[=episodic_synthetic_cases]{episodic_synthetic_cases()}}. \code{outbreaks = FALSE} generates history
with nothing seeded in it, which is the negative control: every
alarm raised on it is a false one.}

\item{n_cases, k_days, case_free_days}{The \code{"same_place"} rule's
parameters: how many cases, within how many days, and how long a
quiet gap ends one alarm and starts the next.}

\item{baseline_weeks, sd_multiplier}{The \code{"shewhart"} rule's
parameters.}

\item{quiet}{If \code{TRUE} (the default), the per-run progress
\code{\link[=episodic_run_cron]{episodic_run_cron()}} writes is suppressed; a replay is hundreds of
lines of it. Warnings and errors are not suppressed.}
}
\value{
An \code{episodic_validation} object, in the same shape
\code{\link[=episodic_validate_detection]{episodic_validate_detection()}} returns, with \code{meta$method} naming
the rule and \code{meta$config_hash} absent - a rule this simple has no
configuration to hash.
}
\description{
A sensitivity of 0.8 and one false alarm per two thousand
stream-weeks mean very little on their own. They mean a great deal
next to what a rule anyone could write in an afternoon gets on the
same data. This runs such a rule over exactly the same generated
history, week by week, and reports it through the same matching and
the same metrics as \code{\link[=episodic_validate_detection]{episodic_validate_detection()}}, so the two sit in
one table.
}
\details{
Two rules are available:

\describe{
\item{\code{"same_place"}}{Any \code{n_cases} cases of one pathogen at one
place within \code{k_days}. A place is a ward inside a hospital and the
institution itself everywhere else, which is what
\code{episodic_detect_same_place()} watches. Cases at one place more
than \code{case_free_days} apart belong to separate alarms.}
\item{\code{"shewhart"}}{A plain control limit on weekly counts: the
whole catchment's count for one pathogen in the week just
completed, against the mean plus \code{sd_multiplier} standard
deviations of the \code{baseline_weeks} weeks before it. Consecutive
alarming weeks are one alarm, not one each.}
}

Neither reconciles, scores, suppresses or ages anything, which is the
point: they are the floor a four-detector design has to clear to be
worth its complexity. Their false-alarm denominator is their own -
places watched, or pathogens watched - and not EpiSODIC's stream
count, so read \code{denominator} before comparing two rates.
}
\examples{
\donttest{
naive <- episodic_validate_comparator()
naive$summary[naive$summary$metric == "sensitivity", ]
}
}
\seealso{
\code{\link[=episodic_validate_detection]{episodic_validate_detection()}}
}
