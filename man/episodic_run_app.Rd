% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_app.R
\name{episodic_run_app}
\alias{episodic_run_app}
\title{Run the EpiSODIC Shiny app}
\usage{
episodic_run_app(
  db_path = Sys.getenv("EPISODIC_DB", unset = NA),
  lang = "nl",
  ...
)
}
\arguments{
\item{db_path}{Path to a SQLite database, or a \verb{mysql://} DSN (see
\code{\link[=episodic_db_dsn_mariadb]{episodic_db_dsn_mariadb()}}). Defaults to the \code{EPISODIC_DB}
environment variable.}

\item{lang}{Default session language, \code{"nl"} (default) or \code{"en"}.}

\item{...}{Passed on to \code{\link[shiny:runApp]{shiny::runApp()}} (e.g. \code{port}, \code{host}).}
}
\value{
Invisible; called for its side effect of starting the app.
}
\description{
Read-only for anonymous visitors: signing in is only required to
classify a cluster. Serves the cluster dossier and the Streams
overview against a database already populated by
\code{\link[=episodic_run_cron]{episodic_run_cron()}}.
}
\examples{
\dontrun{
# opens a blocking, interactive Shiny session - see episodic_demo() for a
# one-call, populated version of this same example
episodic_run_app("/path/to/episode.sqlite")
}
}
