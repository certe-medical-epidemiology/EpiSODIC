% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_app.R
\name{episodic_run_app}
\alias{episodic_run_app}
\title{Open the EpiSODIC dashboard}
\usage{
episodic_run_app(
  db_path = Sys.getenv("EPISODIC_DB", unset = NA),
  lang = Sys.getenv("EPISODIC_LANGUAGE"),
  ...
)
}
\arguments{
\item{db_path}{Path to the EpiSODIC database: a SQLite file, or a
MariaDB/MySQL DSN (see \code{\link[=episodic_db_dsn_mariadb]{episodic_db_dsn_mariadb()}}). Defaults to the
\code{EPISODIC_DB} environment variable.}

\item{lang}{Dashboard language, fixed for the whole running app - there
is no in-app language switcher. One of \code{"en"}, \code{"ar"}, \code{"nl"}, \code{"fr"},
\code{"de"}, \code{"hi"}, \code{"zh"}, or \code{"es"}. Defaults to the \code{EPISODIC_LANGUAGE}
environment variable, falling back to \code{"en"} if that is unset.}

\item{...}{Passed on to \code{\link[shiny:runApp]{shiny::runApp()}}, e.g. \code{port} or \code{host}.}
}
\value{
Invisible; called for its side effect of starting the app. This
call blocks until the app is stopped.
}
\description{
Launches the Shiny dashboard against a database already populated by
\code{\link[=episodic_run_cron]{episodic_run_cron()}}: an overview of monitored surveillance streams and
each cluster's dossier, with charts, its narrative interpretation, and
an assessment form. Anyone can browse the dashboard; signing in is only
required to record an assessment or classify a cluster.
}
\details{
If you just want to explore EpiSODIC without setting anything up first,
use \code{\link[=episodic_demo]{episodic_demo()}} instead - it populates a database with synthetic
data and calls this function for you.
}
\examples{
\dontrun{
# opens a blocking, interactive dashboard session; see episodic_demo()
# for a one-call version that also creates a populated demo database
episodic_run_app("/path/to/episodic.sqlite")
}
}
