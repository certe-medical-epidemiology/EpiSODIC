% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mo_lookup.R
\name{episode_mo_resolve}
\alias{episode_mo_resolve}
\title{Resolve a pathogen code to an \code{AMR::as.mo()} identity}
\usage{
episode_mo_resolve(mo_code, mo_name, mo_rank)
}
\arguments{
\item{mo_code}{The \code{mo_code} as it appears in \code{pathogen_config.csv}.}

\item{mo_name}{The human-readable organism name from the same file.}

\item{mo_rank}{The taxonomic rank from the same file (used only as a
fallback when \code{AMR} is unavailable).}
}
\value{
A list with elements \code{mo_code}, \code{mo_name} and \code{mo_rank}.
}
\description{
\code{episode_pathogen_config.csv} ships placeholder \code{mo_code} values (see
\code{QUESTIONS.md} item 18), since real \code{AMR::as.mo()} codes cannot be
generated without the \code{AMR} package. When \code{AMR} is installed, this
resolves the human-readable \code{mo_name} through \code{AMR::as.mo()} and returns
the real code and taxonomic rank, so that \code{episode_stream} is keyed on the
genuine taxonomy wherever possible. Without \code{AMR}, the placeholder code
and the rank recorded in the config file are returned unchanged, which is
enough to run the bundled demo but is not a substitute for the real
lookup.
}
