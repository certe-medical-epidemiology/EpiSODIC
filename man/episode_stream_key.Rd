% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lattice_stream_key.R
\name{episode_stream_key}
\alias{episode_stream_key}
\title{Compute a deterministic \code{stream_key} for a stream definition}
\usage{
episode_stream_key(
  level,
  pathogen,
  care_line = NA,
  region_code = NA,
  institution_id = NA,
  ward = NA
)
}
\arguments{
\item{level}{One of the five lattice levels, e.g. \code{"pathogen_ward"}.}

\item{pathogen}{The raw lab-provided pathogen string, used as-is (no
taxonomy resolution). See \code{QUESTIONS.md} item 22.}

\item{care_line}{One of \code{"first"}, \code{"second"}, \code{"other"}, \code{"unknown"}, or
\code{NA} if care line does not apply at this level.}

\item{region_code}{The region/area code, or \code{NA}.}

\item{institution_id}{The institution id for hospital streams, or \code{NA}.}

\item{ward}{The ward, for L1 (\code{pathogen_ward}) streams, or \code{NA}. Not a
dimension in \code{ARCHITECTURE.md} section 5.1's \code{episode_stream} DDL; see
\code{QUESTIONS.md} item 20 for why it was added.}
}
\value{
A 40-character lowercase hex SHA-1 digest.
}
\description{
\code{stream_key} identifies a monitored slice of the data (ARCHITECTURE.md
section 5.1) and must hash to the same value for the same slice across
runs and across hosts, regardless of insertion order. The detector is
deliberately not part of the key, since both detectors watch the same
stream.
}
\details{
Algorithm (see \code{QUESTIONS.md} item 14, not specified by the
architecture beyond "deterministic hash of the defining dimensions"):
SHA-1 of the dimension values, coerced to character, \code{NA} mapped to the
literal string \code{"NA"}, joined with \code{"|"} in a fixed field order.
}
