% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/institution_activity.R
\name{episodic_check_institution_activity}
\alias{episodic_check_institution_activity}
\title{Check your hospital activity data before you hand it to EpiSODIC}
\usage{
episodic_check_institution_activity(institution_activity)
}
\arguments{
\item{institution_activity}{Your hospital activity data: a data frame
or \code{tibble} with \code{institution_key}, \code{period_start}, \code{period_end},
\code{patient_days} (nullable \code{admissions}, \code{n_beds}, \code{source}) - see
\code{\link[=episodic_synthetic_institution_activity]{episodic_synthetic_institution_activity()}} for the shape - or a
zero-argument function returning one.}
}
\value{
A data frame of findings with class \code{episodic_case_check}, the
same shape \code{\link[=episodic_check_cases]{episodic_check_cases()}} returns. Zero rows means the
data set passed every check.
}
\description{
The same purpose as \code{\link[=episodic_check_cases]{episodic_check_cases()}}, for the optional
\code{institution_activity} feed: every check
\code{\link[=episodic_run_cron]{episodic_run_cron()}} would refuse a run over, reported instead of
thrown, plus a couple of things worth a look. Nothing is written, no
database is needed.
}
\details{
Unlike the case and denominator feeds, an \code{institution_key} that
matches no institution in your case data is not a problem here - it
is deliberately allowed, and only ever counted and warned about at
run time (see \code{episodic_institution_activity_load()}), since the two
feeds need not be perfectly synchronised and this function has no
case data to compare against in the first place.
}
\examples{
institutions <- data.frame(
  institution_key = "HOSP-1", institution_type = "hospital", n_beds = 320
)
activity <- episodic_synthetic_institution_activity(
  institutions,
  start_date = as.Date("2025-01-01"), end_date = as.Date("2025-03-31")
)
episodic_check_institution_activity(activity)
}
\seealso{
\code{\link[=episodic_check_cases]{episodic_check_cases()}} for the case data feed,
\code{episodic_check_denominators()} for the positivity feed.
}
