% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_cron.R
\name{episode_run_cron}
\alias{episode_run_cron}
\title{Run one detection cycle}
\usage{
episode_run_cron(
  db_path,
  ingest_source_fn = episode_ingest_source_synthetic,
  episode_config_path = Sys.getenv("EPISODE_CONFIG", unset = NA),
  host = Sys.info()[["nodename"]],
  account = Sys.info()[["user"]],
  run_date = Sys.Date()
)
}
\arguments{
\item{db_path}{Path to the SQLite database file. Created if it does not
exist.}

\item{ingest_source_fn}{A zero- or one-argument function returning a
data frame satisfying the ingestion interface. Defaults to the bundled
synthetic generator, the only implementation shipped in this
environment (\code{certedb::get_diver_data()} cannot be called here; see
\code{QUESTIONS.md} item 11).}

\item{episode_config_path}{Passed to \code{\link[=episode_config_resolve]{episode_config_resolve()}}.}

\item{host, account}{Recorded on \code{episode_detection_run}; default to the
process's own host/account (ARCHITECTURE.md section 12: this is not an
identity source for assessors, only for the run record).}

\item{run_date}{The date to treat as "today" for closure/eligibility
calculations; defaults to the system date, injectable for tests.}
}
\value{
Invisibly, the \code{run_id} of the completed run.
}
\description{
The cron entry point (ARCHITECTURE.md section 3, MILESTONES.md M1 step
10). Ingests, enumerates streams, detects, reconciles and persists, all
inside one transaction, so a partially failed run leaves no partial
state and a retry is safe (standing brief hard rule, reconciliation
properties list in \code{MILESTONES.md}).
}
\details{
Configuration is read from \code{EPISODE_CONFIG} (standing brief hard rule 2);
the resolved configuration's hash and full snapshot are written to
\code{episode_detection_run} so that any result is explainable from the
database alone (ARCHITECTURE.md section 7.4).
}
