% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schema_migrate.R
\name{episodic_db_dsn_mariadb}
\alias{episodic_db_dsn_mariadb}
\alias{episodic_db_dsn_mysql}
\title{Connect EpiSODIC to a MariaDB or MySQL Server}
\usage{
episodic_db_dsn_mariadb(host, dbname, user, password, port = 3306L)

episodic_db_dsn_mysql(host, dbname, user, password, port = 3306L)
}
\arguments{
\item{host}{Server hostname or IP address.}

\item{dbname}{Database (schema) name.}

\item{user, password}{Credentials.}

\item{port}{TCP port. Defaults to \code{3306}.}
}
\value{
A single string, ready to pass as \code{db_path} or to
\code{Sys.setenv(EPISODIC_DB = ...)}.
}
\description{
EpiSODIC stores its data in either a SQLite file (the default, and all
you need for a single-server deployment) or a MariaDB/MySQL database.
This function builds the connection string ("DSN") for the latter, so
you never have to hand-assemble one or worry about special characters
in your password breaking it. Use the resulting string as \code{db_path}
anywhere EpiSODIC expects one, or store it in the \code{EPISODIC_DB}
environment variable. \code{episodic_db_dsn_mysql()} is an identical alias -
use whichever name matches the server you run.
}
\examples{
episodic_db_dsn_mariadb(
  host = "db.internal", dbname = "episodic",
  user = "episodic_app", password = "s3cr3t!"
)
}
