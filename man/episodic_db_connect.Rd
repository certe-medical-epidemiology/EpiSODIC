% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schema_migrate.R
\name{episodic_db_connect}
\alias{episodic_db_connect}
\title{Connect to an existing EpiSODIC database}
\usage{
episodic_db_connect(path)
}
\arguments{
\item{path}{Path to an existing SQLite file, or a \verb{mysql://} DSN (see
\code{\link[=episodic_db_dsn_mariadb]{episodic_db_dsn_mariadb()}}) pointing at an existing MariaDB/MySQL
database.}
}
\value{
An open \link[DBI:DBIConnection-class]{DBI::DBIConnection}.
}
\description{
Opens a connection to a database you have already set up with
\code{\link[=episodic_db_create]{episodic_db_create()}}, with the settings EpiSODIC needs enabled (for
SQLite: WAL journal mode, a busy timeout, and foreign key enforcement).
Remember to disconnect with \code{\link[DBI:dbDisconnect]{DBI::dbDisconnect()}} when you are done.
}
\examples{
db_path <- tempfile(fileext = ".sqlite")
con <- episodic_db_create(db_path)
DBI::dbDisconnect(con)
con <- episodic_db_connect(db_path)
DBI::dbDisconnect(con)
file.remove(db_path)
}
