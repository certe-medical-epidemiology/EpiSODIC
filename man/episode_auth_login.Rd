% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auth.R
\name{episode_auth_login}
\alias{episode_auth_login}
\title{Attempt to log in}
\usage{
episode_auth_login(con, username, password)
}
\arguments{
\item{con}{A \link[DBI:DBIConnection-class]{DBI::DBIConnection}.}

\item{username, password}{Login credentials.}
}
\value{
A list: \code{ok} (logical), and if \code{ok} is \code{TRUE}, \code{user} (the
account row) and \code{must_change} (logical, whether a forced password
change is due).
}
\description{
Verifies \code{username}/\code{password} against the account's current password
hash and, on success, records a
\code{login} event. Deliberately silent about \emph{why} a login failed (unknown
username vs. wrong password vs. inactive account are folded into the
same generic outcome) - the login exists to attribute assessments made
by people already inside the building, not to resist an attacker who
would learn more from a distinguishing error message.
}
