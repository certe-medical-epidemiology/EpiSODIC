% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db_cron_write.R
\name{db_cron_write}
\alias{db_cron_write}
\alias{episode_db_pathogen_config_load}
\alias{episode_db_institution_upsert}
\alias{episode_db_institution_activity_upsert}
\alias{episode_db_stream_upsert}
\alias{episode_db_case_insert_new}
\alias{episode_db_reporting_triangle_upsert}
\alias{episode_db_detection_insert}
\alias{episode_db_detection_set_cluster}
\alias{episode_db_cluster_insert}
\alias{episode_db_cluster_update}
\alias{episode_db_cluster_increment_runs_since_detected}
\alias{episode_db_cluster_set_merged_into}
\alias{episode_db_cluster_case_link}
\alias{episode_db_run_start}
\alias{episode_db_run_finish}
\title{Cron-side writers}
\usage{
episode_db_pathogen_config_load(con, pathogen_config)

episode_db_institution_upsert(
  con,
  institution_key,
  display_name,
  institution_type,
  care_line,
  municipality = NA,
  pc4 = NA,
  n_beds = NA,
  is_monitored = FALSE
)

episode_db_institution_activity_upsert(
  con,
  institution_id,
  period_start,
  period_end,
  patient_days = NA,
  admissions = NA,
  n_beds = NA,
  source = NA
)

episode_db_stream_upsert(
  con,
  stream_key,
  level,
  mo_code,
  mo_name,
  mo_rank,
  care_line = NA,
  region_code = NA,
  institution_id = NA,
  ward = NA,
  denominator = "none",
  severity_weight = 1,
  observed_date
)

episode_db_case_insert_new(con, cases, run_id)

episode_db_reporting_triangle_upsert(
  con,
  stream_id,
  sample_date,
  run_date,
  n_cases
)

episode_db_detection_insert(
  con,
  run_id,
  stream_id,
  detector,
  first_day,
  last_day,
  n_cases,
  expected = NA,
  upperbound = NA,
  params_json,
  cluster_id = NA
)

episode_db_detection_set_cluster(con, detection_id, cluster_id)

episode_db_cluster_insert(
  con,
  stream_id,
  first_day,
  last_day,
  n_cases,
  expected = NA,
  excess = NA,
  ratio = NA,
  priority_score,
  detector_agreement,
  run_id
)

episode_db_cluster_update(
  con,
  cluster_id,
  first_day,
  last_day,
  n_cases,
  expected = NA,
  excess = NA,
  ratio = NA,
  priority_score,
  detector_agreement,
  run_id,
  changed_since_assessment = NULL
)

episode_db_cluster_increment_runs_since_detected(con, cluster_id)

episode_db_cluster_set_merged_into(con, cluster_id, merged_into)

episode_db_cluster_case_link(con, cluster_id, case_id)

episode_db_run_start(con, host, account, attempt_no = 1L)

episode_db_run_finish(
  con,
  run_id,
  status,
  n_streams = NA,
  n_detections = NA,
  n_signals_new = NA,
  n_signals_updated = NA,
  code_version = NA,
  pkg_versions = NA,
  config_hash = NA,
  config_snapshot = NA,
  error_text = NA
)
}
\arguments{
\item{con}{A \link[DBI:DBIConnection-class]{DBI::DBIConnection}.}

\item{pathogen_config}{A data frame matching \code{inst/config/pathogen_config.csv}.}

\item{institution_key, display_name, institution_type, municipality, is_monitored}{Columns of \code{episode_institution}, see ARCHITECTURE.md section 5.4.1.}

\item{care_line}{One of \code{"first"}, \code{"second"}, \code{"other"}, \code{"unknown"}, or \code{NA}.}

\item{pc4}{A PC4 postcode, or \code{NA}.}

\item{institution_id}{An \code{episode_institution} id.}

\item{period_start, period_end}{Activity period bounds (dates).}

\item{patient_days, admissions, n_beds}{Activity counts, or \code{NA}.}

\item{source}{Free-text provenance, or \code{NA}.}

\item{stream_key}{A 40-character \code{stream_key}.}

\item{level}{One of the five lattice levels.}

\item{mo_code, mo_name, mo_rank}{Pathogen identity fields.}

\item{region_code}{A region code, or \code{NA}.}

\item{ward}{A ward, for \code{pathogen_ward} streams, or \code{NA}.}

\item{denominator}{One of \code{"none"}, \code{"tests"}, \code{"population"}, \code{"patient_days"}.}

\item{severity_weight}{A severity weight, 0-1.}

\item{observed_date}{The date this stream was observed on, updates \code{first_seen}/\code{last_seen}.}

\item{cases}{A data frame of cases to insert (\code{episode_ingest_run()}'s deduplicated batch).}

\item{run_id}{A \code{run_id}.}

\item{stream_id}{A \code{stream_id}.}

\item{sample_date, run_date}{Reporting-triangle dates.}

\item{n_cases}{A case count.}

\item{detector}{One of the detector enum values.}

\item{first_day, last_day}{A detection or cluster interval.}

\item{expected, upperbound}{Statistical detector output, or \code{NA}.}

\item{params_json}{JSON-serialised detector attributes.}

\item{cluster_id}{A \code{cluster_id}, or \code{NA}.}

\item{detection_id}{A \code{detection_id}.}

\item{excess, ratio}{Cluster statistics, or \code{NA}.}

\item{priority_score}{A priority score, 0-100.}

\item{detector_agreement}{Count of distinct detectors that fired.}

\item{changed_since_assessment}{Logical, or \code{NULL} to leave unchanged.}

\item{merged_into}{The surviving \code{cluster_id} this cluster merged into.}

\item{case_id}{A \code{case_id}.}

\item{host, account}{Recorded on \code{episode_detection_run}.}

\item{attempt_no}{The run's attempt number.}

\item{status}{One of \code{episode_detection_run.status}.}

\item{n_streams, n_detections, n_signals_new, n_signals_updated}{Run summary counts.}

\item{code_version}{The installed EpiSODE version.}

\item{pkg_versions}{JSON-serialised package versions.}

\item{config_hash, config_snapshot}{The resolved configuration's hash and snapshot.}

\item{error_text}{Error text for a failed run, or \code{NA}.}
}
\value{
The \code{institution_id} of the inserted or existing row.
}
\description{
The cron owns the facts (ARCHITECTURE.md section 5.0) and may upsert.
These functions are the only place in the package that write to
\code{episode_stream}, \code{episode_institution}, \code{episode_institution_activity},
\code{episode_case}, \code{episode_reporting_triangle}, \code{episode_denominator},
\code{episode_mo_determination}, \code{episode_detection}, \code{episode_cluster},
\code{episode_cluster_case}, \code{episode_detection_run} and (for pre-renders)
\code{episode_report_render}. See \code{R/db_app_write.R} for the insert-only
counterparts.
}
