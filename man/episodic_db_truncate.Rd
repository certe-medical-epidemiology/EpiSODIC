% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schema_migrate.R
\name{episodic_db_truncate}
\alias{episodic_db_truncate}
\title{Empty Every EpiSODIC Table, Keeping the Schema Itself}
\usage{
episodic_db_truncate(path)
}
\arguments{
\item{path}{Path to an existing SQLite file, or a \verb{mysql://} DSN (see
\code{\link[=episodic_db_dsn_mariadb]{episodic_db_dsn_mariadb()}}) pointing at an existing MariaDB/MySQL
database.}
}
\value{
Invisibly, the character vector of tables that were
truncated - \code{character(0)} if you cancelled, or if the database had
no EpiSODIC tables to begin with.
}
\description{
A hard reset back to "freshly created, no data" - every row in every
EpiSODIC table is deleted, but the tables, indexes and constraints
stay exactly as \code{\link[=episodic_db_create]{episodic_db_create()}} built them, so the database is
immediately ready for a new \code{\link[=episodic_run_cron]{episodic_run_cron()}} without a schema
migration. This includes \code{episodic_app_user}: dashboard accounts are
data too, and are deleted along with everything else - there is
nothing this function leaves behind to sign in with afterwards.
}
\details{
Deliberately hard to trigger by accident:

\describe{
\item{Interactive only}{Refuses outright under \code{\link[=interactive]{interactive()}}
\code{FALSE} - a script, a cron job, or any other unattended context
can never reach the confirmation prompt, so it can never reach
the deletion either.}
\item{Typed confirmation}{Prints exactly what is about to be
deleted (every table, and its row count) and then requires you to
type the database's own name back at a prompt. A bare yes/no is
answerable on autopilot; typing the name back is not.}
}
}
\examples{
\dontrun{
episodic_db_truncate(db_path)
}
}
