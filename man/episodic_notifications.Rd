% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/notify.R
\name{episodic_notifications}
\alias{episodic_notifications}
\title{How notifications work}
\description{
When \code{\link[=episodic_run_cron]{episodic_run_cron()}} detects new clusters or fails, it can
notify your team through one or more channels: ntfy, SMTP, sendmail,
Microsoft 365, Teams (Power Automate Workflow webhook), or Slack.
Notifications are configured in the instance YAML configuration file
(the same file \code{EPISODIC_CONFIG} points at), under a \code{notifications}
key. The notification settings are deliberately excluded from the
detection \code{config_hash}, so changing them never alters reproducibility
and secrets never reach the \code{config_snapshot} stored on each run.
}
\details{
Notifications are dispatched \emph{after} the detection transaction has
committed: a failed notification never rolls back a successful run.
Errors during dispatch are logged via \code{episodic_trace()} and never
stop the run from finishing.
}
