% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_cron.R
\name{episodic_run_cron}
\alias{episodic_run_cron}
\title{Run one surveillance detection cycle}
\usage{
episodic_run_cron(
  cases,
  denominators = NULL,
  institution_activity = NULL,
  episodic_config_path = Sys.getenv("EPISODIC_CONFIG", unset = NA),
  db_path = Sys.getenv("EPISODIC_DB"),
  host = Sys.info()[["nodename"]],
  account = Sys.info()[["user"]],
  run_date = Sys.Date(),
  debug = FALSE
)
}
\arguments{
\item{cases}{Your laboratory data: a data frame or \code{tibble} in the
shape \link{episodic_case_data} describes, or a zero-argument function
that returns one. Defaults to the bundled synthetic generator, useful
for demos and testing but not real surveillance.}

\item{denominators}{Optional: your testing-volume data, in the same
form as \code{cases} - normally a data set, a function if it has to be
produced at run time (see \code{\link[=episodic_synthetic_denominators]{episodic_synthetic_denominators()}} for the
expected shape). Leave as \code{NULL} (the default) if you have none to
supply - positivity panels simply stay blank.}

\item{institution_activity}{Optional: your hospital patient-days
data (see \code{\link[=episodic_synthetic_institution_activity]{episodic_synthetic_institution_activity()}} for the
expected shape), normally as a data set, or as a function taking the
current institutions table. Leave as \code{NULL} (the default) if you have
none - detection falls back to raw case counts.}

\item{episodic_config_path}{Passed to \code{\link[=episodic_config_resolve]{episodic_config_resolve()}}.}

\item{db_path}{Path to the EpiSODIC database: a SQLite file (created
automatically if it does not exist yet) or a MariaDB/MySQL DSN (see
\code{\link[=episodic_db_dsn_mariadb]{episodic_db_dsn_mariadb()}}).}

\item{host, account}{Recorded with the run for audit purposes; default
to the current machine and account.}

\item{run_date}{The date to treat as "today". Defaults to the system
date; mainly useful to override in tests.}

\item{debug}{If \code{TRUE}, print a lot more than the phase-by-phase
progress this function always writes: \code{sessionInfo()}, the versions
of every package a fatal (non-catchable) crash is most likely to
originate in, memory snapshots, per-stream detail inside the
detection loop, and - for the calls implicated so far in a known
MariaDB-only crash (\code{episodic_app_density()}, the population-vector
lookup, the trend/detection writes, the assessment-event lookups,
and \code{episodic_spatial_concentration()}'s own input) - the exact SQL
and every bound parameter's value, class and encoding immediately
before each such call runs, not only once it returns. Meant for
chasing exactly the kind of failure that leaves no R-level error
behind at all - a crashed session, a run that silently never
returns - where the normal progress trace does not narrow things
down enough on its own. Noisy; leave off for routine scheduled
runs.}
}
\value{
Invisibly, the \code{run_id} of the completed run. The run's row in
\code{episodic_detection_run} holds its status, the per-feed load counts,
and \code{error_text} if it failed. Case data that does not satisfy the
contract throws instead of returning - the run row is still written,
with \code{status = "failed"} and the same message in \code{error_text}.
}
\description{
This is the function you schedule to run regularly (e.g. daily, via
cron): it pulls in new laboratory data, checks every monitored stream
for statistical aberrations with the configured detectors, reconciles
the results into cluster dossiers for the board to assess, and records
everything in the database. A run either completes in full or leaves no
trace at all - it runs inside a single database transaction, so a
failed run is always safe to simply retry.
}
\details{
EpiSODIC never connects to your laboratory system directly. You extract
and transform your own data beforehand, and hand it over as a plain data
frame or \code{tibble}: \code{cases} for the laboratory results themselves (see
\link{episodic_case_data} for the required columns and their allowed
values), and optionally \code{denominators} and \code{institution_activity} for
testing volume and hospital activity. A data set is the normal case; if
producing the data only makes sense at run time (a live database query,
for instance), a zero-argument function returning one is accepted just
as well - see \code{\link[=episodic_resolve_data]{episodic_resolve_data()}}.

The exact detection settings used are recorded with the run (see
\code{\link[=episodic_config_hash]{episodic_config_hash()}}), so any past result can always be traced back
to the configuration that produced it.

So is what each feed delivered. Before the run writes anything, your
case data goes through \code{\link[=episodic_check_cases]{episodic_check_cases()}}. Structural problems -
a missing column, a value outside the allowed set, a date that does not
read as a date - stop the run with an error naming every offending
column, its values and the rows they are in, and are recorded on the
run as well, so the reason is visible both where the run was started
and in the dashboard's activity screen. Advisory findings are mentioned
once and the run proceeds. Run \code{\link[=episodic_check_cases]{episodic_check_cases()}} on your extract
yourself to see all of it without starting a run at all. A run that
fails later, for any other reason, records the reason and warns rather
than returning quietly. Rows
that are merely unmatched are counted rather than dropped in silence:
institution activity whose \code{institution_key} matches no known
institution is skipped with a warning, its count recorded, and the run
finishes \code{"partial"} instead of \code{"success"}. Both are complete runs the
dashboard reads from; \code{"partial"} says go and look at why rows were
skipped. \code{episodic_detection_run} carries the counts (\code{n_cases_supplied},
\code{n_cases_inserted}, \code{n_activity_skipped}, and the rest).
}
\section{Check your data before you run anything}{


Do not find out from an empty dashboard. Run \code{\link[=episodic_check_cases]{episodic_check_cases()}}
on your extract - it needs no database, changes nothing, and reports
every problem it finds at once, with the rows and values involved and
what to do about each:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{cases <- my_extract_and_transform_function()
episodic_check_cases(cases)
}\if{html}{\out{</div>}}

It also reports what is merely worth a look: one pathogen spelled two
ways (two streams instead of one), no \code{ward} on any hospital row (no
ward-level detection), a \code{patient_key} that never repeats (no
deduplication), postcodes the map cannot place, sample dates in the
future. \code{\link[=episodic_validate_cases]{episodic_validate_cases()}} runs the same checks but throws,
for use in a script; \code{\link[=episodic_run_cron]{episodic_run_cron()}} runs it for you before every
run, and refuses to start on data it cannot use, naming what to fix.
}

\examples{
\donttest{
db_path <- tempfile(fileext = ".sqlite")
cases <- episodic_synthetic_cases(
  start_date = as.Date("2025-01-01"), end_date = as.Date("2025-03-31")
)
run_id <- episodic_run_cron(db_path = db_path, cases = cases)
file.remove(db_path)
}
}
\seealso{
\code{\link[=episodic_check_cases]{episodic_check_cases()}} to see what EpiSODIC makes of your
extract before you schedule anything, and \link{episodic_case_data} for
the contract it checks against.
}
