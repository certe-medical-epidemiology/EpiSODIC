% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/score_priority.R
\name{episode_priority_score}
\alias{episode_priority_score}
\title{Priority score}
\usage{
episode_priority_score(
  excess = NA,
  ratio = NA,
  severity_weight = 1,
  growth_slope = 0,
  detector_agreement = 1,
  n_detectors = 1,
  density_ratio = NA,
  spatial_concentration = 0,
  weights
)
}
\arguments{
\item{excess}{Observed minus upperbound (may be \code{NA}).}

\item{ratio}{Observed over expected, capped at 5 (may be \code{NA}).}

\item{severity_weight}{From \code{episode_stream}/\code{episode_pathogen_config}.}

\item{growth_slope}{Slope of the last three aggregation periods.}

\item{detector_agreement, n_detectors}{Count of detectors that fired, and
how many exist in total.}

\item{density_ratio}{Incidence per 1,000 patient-days versus baseline,
or \code{NA} when no patient-day denominator exists for this stream.}

\item{spatial_concentration}{A 0-1 concentration measure across PC4
(Gini or top-1 share).}

\item{weights}{A list from \code{config$priority_score$weights}.}
}
\value{
A single numeric score, 0-100.
}
\description{
ARCHITECTURE.md section 8.1: a weighted mean of seven rescaled
components, 0-100. The density component only applies where a
patient-day denominator exists; elsewhere the remaining weights
renormalise rather than depressing the score for a purely structural
reason (section 8.1, "hospital signals would systematically outrank
community ones").
}
\details{
This is priority ranking, not exclusion (section 8, item 5): the score
never removes a candidate from the queue, it only orders it. Weight
calibration against real signal volume needs an instance that has
actually run for a while (see \code{QUESTIONS.md}); the values shipped in
\code{inst/config/default.yaml} are the architecture's stated defaults,
unmodified.
}
