% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/denominators.R
\name{episodic_check_denominators}
\alias{episodic_check_denominators}
\title{Check your positivity data before you hand it to EpiSODIC}
\usage{
episodic_check_denominators(denominators)
}
\arguments{
\item{denominators}{Your positivity data: a data frame or \code{tibble}
with \code{pathogen}, \code{sample_date}, \code{care_line}, \code{area_code} and
\code{n_tests} (see \code{\link[=episodic_synthetic_denominators]{episodic_synthetic_denominators()}} for the shape),
or a zero-argument function returning one.}
}
\value{
A data frame of findings with class \code{episodic_case_check}, the
same shape \code{\link[=episodic_check_cases]{episodic_check_cases()}} returns. Zero rows means the
data set passed every check.
}
\description{
The same purpose as \code{\link[=episodic_check_cases]{episodic_check_cases()}}, for the optional
\code{denominators} feed: every check \code{episodic_validate_denominators()}
would throw on, reported instead of thrown - what is wrong, how many
rows, which ones, and what to do about it - plus a couple of things
that are allowed but worth a look. Nothing is written, no database is
needed. Run this while building your extract step, or when a run's
positivity panels come out empty or look wrong.
}
\examples{
denom <- episodic_synthetic_denominators(
  start_date = as.Date("2025-01-01"), end_date = as.Date("2025-03-31")
)
episodic_check_denominators(denom)
}
\seealso{
\code{\link[=episodic_check_cases]{episodic_check_cases()}} for the case data feed,
\code{episodic_check_institution_activity()} for the hospital activity
feed.
}
