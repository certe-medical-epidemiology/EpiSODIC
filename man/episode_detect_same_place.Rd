% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detect_same_place.R
\name{episode_detect_same_place}
\alias{episode_detect_same_place}
\title{\code{same_place} detector}
\usage{
episode_detect_same_place(con, cases, institutions, config)
}
\arguments{
\item{con}{A \link[DBI:DBIConnection-class]{DBI::DBIConnection}.}

\item{cases}{A data frame of cases to scan, with \code{pathogen},
\code{institution_id}, \code{ward}, \code{sample_date}.}

\item{institutions}{A data frame from \code{episode_db_institutions()}.}

\item{config}{The resolved configuration; uses \code{config$same_place}.}
}
\value{
A data frame of detection records (\code{episode_detection_record()}
shape) plus a \code{stream_id} column, one row per hit.
}
\description{
Needs no baseline: \emph{n} or more cases of the same organism at the same
place within \emph{k} days (ARCHITECTURE.md section 7.2). Inside hospitals the
rule runs on ward, the real transmission unit; everywhere else (long-term
care, out-of-hours services, general practice) it runs on institution,
since those places do not receive their own statistically-modelled
streams. The stream a hit belongs to is created here if it does not
already exist, via the same \code{episode_db_stream_upsert()} used by lattice
enumeration, so \code{same_place} detections reconcile into the same
\code{episode_stream}/\code{episode_cluster} tables as every other detector.
}
