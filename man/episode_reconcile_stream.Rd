% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reconcile.R
\name{episode_reconcile_stream}
\alias{episode_reconcile_stream}
\title{Cluster reconciliation}
\usage{
episode_reconcile_stream(
  con,
  stream_id,
  detections,
  case_free_days,
  run_id,
  close_after_runs,
  priority_score_fn,
  has_assessment_fn,
  verdict_fn
)
}
\arguments{
\item{con}{A \link[DBI:DBIConnection-class]{DBI::DBIConnection}.}

\item{stream_id}{The stream to reconcile.}

\item{detections}{A data frame of this run's detections for this stream
(already inserted into \code{episode_detection} with their \code{detection_id}).}

\item{case_free_days}{The organism's case-free interval, from
\code{episode_pathogen_config}.}

\item{run_id}{The current \code{run_id}.}

\item{close_after_runs}{From \code{config$reconciliation$close_after_runs}.}

\item{priority_score_fn}{A function \code{(candidate) -> numeric(1)} computing
the priority score for a candidate episode; injected so reconciliation
does not depend on \code{R/score_*.R} implementation details.}

\item{has_assessment_fn}{A function \code{(cluster_id) -> logical(1)},
\code{TRUE} if the cluster carries any assessment event.}

\item{verdict_fn}{A function \verb{(cluster_id) -> character(1) or NA}, the
latest verdict for a cluster, used to decide auto-closure eligibility.}
}
\value{
Invisibly, a list with \code{n_new}, \code{n_updated}, \code{n_merged}.
}
\description{
Run per stream, after detection, inside the run transaction
(ARCHITECTURE.md section 6). This is the load-bearing algorithm of the
whole system: no detector emits a stable cluster identity on its own, so
mapping today's detections onto persistent, real-world clusters is what
makes the rest of the application possible.
}
\details{
Algorithm, matching section 6 verbatim:
\enumerate{
\item Collect this run's detections for the stream across all detectors.
\item Merge detections whose intervals overlap into candidate episodes,
recording \code{detector_agreement} (how many distinct detectors fired).
\item For each candidate, find open clusters in the same stream whose
interval overlaps it, or lies within \code{case_free_days} of it.
\itemize{
\item No match: open a new cluster.
\item One match: extend it, recount, recompute score; if it already
carries an assessment and the interval or case count changed, flag
\code{changed_since_assessment}.
\item Multiple matches: merge. The oldest surviving cluster absorbs the
others via \code{merged_into}; nothing is deleted and no assessment
history is lost.
}
\item Open clusters in the stream with no candidate this run get
\code{runs_since_detected + 1}.
\item Auto-closure: unassessed clusters, or those assessed \code{artefact}/
\code{expected_variation}, close after \code{close_after_runs} runs undetected.
}

This function is idempotent by construction: it is keyed on \code{stream_key}
and interval overlap rather than on insertion order (section 5.3), so
running it twice over the same detections produces no new clusters and
no duplicate rows, and the caller is responsible for wrapping it (and
the detection step that feeds it) in one transaction per run so a
partial failure leaves no partial state.
}
