% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_detection.R
\name{episodic_validate_detection}
\alias{episodic_validate_detection}
\alias{print.episodic_validation}
\title{Measure Detection Against Known Truth}
\usage{
episodic_validate_detection(
  seeds = 1,
  end_date = Sys.Date(),
  history_years = 1,
  evaluation_weeks = 4,
  min_recall = 0.5,
  min_precision = 0.5,
  outbreaks = TRUE,
  outbreak_offsets = NULL,
  detectors = episodic_validation_detectors(),
  config = NULL,
  quiet = TRUE
)

\method{print}{episodic_validation}(x, ...)
}
\arguments{
\item{seeds}{RNG seeds to replicate over. Seeds are the replicates:
one realisation of a stochastic generator is an anecdote, so the
summary reports medians and interquartile ranges across them.}

\item{end_date}{The last day of generated history, and the last run
the replay makes. Defaults to today, which means two runs on
different days are not comparable; pin it for anything whose numbers
are going to be quoted.}

\item{history_years}{Years of endemic history before the evaluation
window, for the detectors to build a baseline from.}

\item{evaluation_weeks}{Weekly runs to replay.}

\item{min_recall, min_precision}{The matching thresholds described
above.}

\item{outbreaks, outbreak_offsets}{Passed to
\code{\link[=episodic_synthetic_cases]{episodic_synthetic_cases()}}. \code{outbreaks = FALSE} generates history
with nothing seeded in it, which is the negative control: every
alarm raised on it is a false one.}

\item{detectors}{Which detectors to switch on, for a drop-one
analysis. Any of \code{"farrington"}, \code{"mem"}, \code{"same_place"} and
\code{"rare_trigger"}; the rest are switched off in the instance
configuration this writes, so \code{config_hash} records which ran.}

\item{config}{Further instance configuration to write, as a nested
list (e.g. \code{list(farrington = list(alpha = 0.01))}), merged over the
shipped defaults and validated the same way an operator's file is.
It may not set any detector's \code{enabled} key - that is what
\code{detectors} is for, and two mechanisms for one setting is one too
many.}

\item{quiet}{If \code{TRUE} (the default), the per-run progress
\code{\link[=episodic_run_cron]{episodic_run_cron()}} writes is suppressed; a replay is hundreds of
lines of it. Warnings and errors are not suppressed.}

\item{x}{An \code{episodic_validation} object.}

\item{...}{Ignored.}
}
\value{
An \code{episodic_validation} object: a list of
\describe{
\item{\code{outbreaks}}{One row per (seed, seeded outbreak): whether it
was detected, by which cluster, how much of it that cluster
held, how late, and how much of it was still to come at that
moment.}
\item{\code{clusters}}{One row per (seed, cluster raised): its case-set
precision, whether it counts as a true positive, its priority
score, and which detectors fired on it.}
\item{\code{runs}}{One row per (seed, run): streams watched, clusters
raised, and how many of those survived lattice suppression.}
\item{\code{overlap}}{Every (run, cluster, outbreak) that shared a
case, which is the raw material both tables above are derived
from.}
\item{\code{truth}}{What was injected: the outbreak table and the
case-level membership, per seed. Kept with the result so the
matching thresholds can be varied afterwards
(\code{\link[=episodic_validate_rethreshold]{episodic_validate_rethreshold()}}) without replaying anything.}
\item{\code{time_to_detection}}{The Kaplan-Meier curve of days to
detection over the outbreaks that began inside the evaluation
window, with the ones nothing found kept in as right-censored
observations rather than dropped.}
\item{\code{summary}}{The headline numbers, one row per metric and
group.}
\item{\code{meta}}{The package version, the resolved \code{config_hash}, the
thresholds, the seeds, and the denominators every figure rests
on.}
}
}
\description{
The dashboard's Performance screen measures this
instance against its own epidemiologists' verdicts. That is the right
operational metric and a circular one for a paper: it reports what the
board thought of what the system showed them, and an outbreak the
detectors miss never becomes a cluster for anyone to have an opinion
about, so sensitivity cannot be measured that way at all.
}
\details{
This measures the detectors against outbreaks that are known to be
there because they were put there. It generates synthetic history with
\code{\link[=episodic_synthetic_cases]{episodic_synthetic_cases()}}, replays it week by week through
\code{\link[=episodic_run_cron]{episodic_run_cron()}} against a throwaway database, and compares the
clusters that came out against the ground truth that went in.
}
\section{How a cluster is matched to an outbreak}{


On the cases themselves, in both directions, never on overlapping
dates. An endemic winter cluster spanning the same fortnight as a
seeded outbreak overlaps it perfectly in time and shares not one case
with it.
\itemize{
\item An \strong{outbreak is detected} when some cluster's final membership
holds at least \code{min_recall} of the outbreak's cases.
\item A \strong{cluster is a true positive} when at least \code{min_precision} of
its own cases belong to one seeded outbreak. Anything else is a
false alarm.
}

Both thresholds are arguments because both are choices, and a result
that only holds at one particular pair of values is not a result.
Sweep them and see (\verb{data-raw/validation/} does).
}

\section{Why it replays week by week}{


A single run over the whole history says nothing about timeliness: it
sees the outbreak's last case at the same moment as its first. So the
replay steps \code{run_date} forward one week at a time and hands each run
only the cases sampled on or before that date - what a laboratory
extract taken that day would actually have contained.

Two consequences worth knowing before reading any number out of this.
Farrington needs \code{(farrington$b + 1) * 52} weeks of history and MEM
needs two seasons, so an evaluation window that starts before the
baseline requirement is met measures the warm-up rather than the
method: keep \code{history_years} at 4 or more if the statistical detectors
are meant to be running. And all six seeded outbreaks are anchored to
the end of the generated window unless \code{outbreak_offsets} disperses
them, so a prospective evaluation wants them dispersed.
}

\section{Cost}{


Every run refits Farrington for every eligible stream, so the cost is
roughly (seeds x weeks) full detection runs. The defaults are
deliberately small - one seed, a short window, a single year of
history - so this returns in seconds and can be run from an example.
They are a demonstration of the machinery, not a study: at one year of
history the statistical detectors have no baseline to fit against and
report nothing, and one seed is one realisation. The study lives in
\verb{data-raw/validation/}, is measured in minutes to hours, and is not
run by the test suite, by \verb{R CMD check}, or by anything a user
installs.
}

\examples{
\donttest{
result <- episodic_validate_detection()
result$summary[result$summary$metric == "sensitivity", ]
}
}
\seealso{
\code{\link[=episodic_synthetic_ground_truth]{episodic_synthetic_ground_truth()}} for what is being
measured against, and \code{\link[=episodic_run_cron]{episodic_run_cron()}} for the detection run
this replays.
}
