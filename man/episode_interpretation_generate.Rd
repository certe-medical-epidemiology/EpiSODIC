% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interpretation.R
\name{episode_interpretation_generate}
\alias{episode_interpretation_generate}
\title{Generate the interpretation for a cluster}
\usage{
episode_interpretation_generate(cluster, lang = "nl", instance_i18n = NULL)
}
\arguments{
\item{cluster}{A cluster object, see \code{episode_cluster_object()}.}

\item{lang}{Session language, \code{"nl"} or \code{"en"}.}

\item{instance_i18n}{Optional operator overrides, passed to \code{\link[=episode_tr]{episode_tr()}}.}
}
\value{
A list with \code{text} (a character vector, one string per slot that
fired, in slot order) and \code{fired} (a character vector of the fragment
ids that fired, same order) - every fragment records which
condition fired, so the interpretation is always traceable back to
the evidence that produced it.
}
\description{
Generate the interpretation for a cluster
}
