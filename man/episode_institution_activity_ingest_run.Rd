% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingest_institution_activity.R
\name{episode_institution_activity_ingest_run}
\alias{episode_institution_activity_ingest_run}
\title{Ingest optional institution activity (patient-days)}
\usage{
episode_institution_activity_ingest_run(con, activity)
}
\arguments{
\item{con}{A \link[DBI:DBIConnection-class]{DBI::DBIConnection}.}

\item{activity}{A data frame with \code{institution_key}, \code{period_start},
\code{period_end}, \code{patient_days} (nullable \code{admissions}, \code{n_beds},
\code{source}).}
}
\value{
Invisibly, the number of rows written (rows whose
\code{institution_key} does not match a known institution are skipped,
not an error - an operator's activity feed and case feed need not be
perfectly synchronised).
}
\description{
Writes operator-supplied weekly hospital activity to
\code{episode_institution_activity}, keyed on \code{institution_key} (resolved to
\code{institution_id} here, mirroring \code{episode_denominator_ingest_run()}'s
own optional-source pattern). Entirely optional: a site with nothing to
supply here simply never calls this, and L1/L2 Farrington detection
falls back to raw counts - unnormalised, as it always has been, not
broken (ARCHITECTURE.md section 7.1's patient-day normalisation is a
refinement, not a requirement).
}
