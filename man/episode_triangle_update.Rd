% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/triangle_update.R
\name{episode_triangle_update}
\alias{episode_triangle_update}
\title{Reporting triangle}
\usage{
episode_triangle_update(con, stream_id, cases_for_stream, run_date)
}
\arguments{
\item{con}{A \link[DBI:DBIConnection-class]{DBI::DBIConnection}.}

\item{stream_id}{The stream to update the triangle for.}

\item{cases_for_stream}{A data frame of that stream's current cases
(all of them, not just this run's), with \code{sample_date}.}

\item{run_date}{The date of the current run (\code{as.character(Sys.Date())}
in production; injectable for tests).}
}
\value{
Invisibly, the number of \verb{(sample_date, run_date)} rows written.
}
\description{
Because sample date silently becomes receipt date when the physician
leaves it blank, reporting delay cannot be
read off the date columns. It is measured from observed accrual instead:
how many cases with a given sample date were visible on each successive
run date (ARCHITECTURE.md section 5.7).
}
