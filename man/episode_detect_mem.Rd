% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detect_mem.R
\name{episode_detect_mem}
\alias{episode_detect_mem}
\title{\code{mem} seasonal detector}
\usage{
episode_detect_mem(
  cases_for_stream,
  stream_id,
  run_date = Sys.Date(),
  min_seasons = 2L
)
}
\arguments{
\item{cases_for_stream}{A data frame of a single (L5) stream's cases,
with \code{sample_date}.}

\item{stream_id}{The stream these cases belong to.}

\item{run_date}{The date to treat as "today".}

\item{min_seasons}{Minimum number of complete prior seasons required.}
}
\value{
A data frame of detection records (zero or one row).
}
\description{
The Moving Epidemic Method (\code{mem} package - not an R base/CRAN
near-namesake of anything else in this codebase) supplies pre-epidemic
and post-epidemic thresholds from historical seasons, for organisms
flagged \code{mem_applicable} (Influenza A/B, RSV in the shipped
\code{pathogen_config.csv}). Farrington answers whether counts exceed a
statistical expectation; MEM answers a different, more clinically
relevant question for a seasonal pathogen - has the epidemic started
(ARCHITECTURE.md section 7.3).
}
\details{
Runs only against \code{pathogen_region} (L5) streams: MEM needs a stable,
population-level weekly series across several historical seasons to
fit at all, which no ward or single institution has enough volume to
support, and ARCHITECTURE.md itself frames seasonal surveillance as a
population-level question, not an institutional one. \code{same_place} and
Farrington continue to run at every other level exactly as before;
this is additive, not a replacement.

Requires at least two complete prior seasons in the historical matrix
before firing at all (\code{min_seasons}), the same "need real baseline
history first" posture Farrington already takes for its own \code{b}
parameter.
}
