% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/duiding.R
\name{episode_duiding_recommendation}
\alias{episode_duiding_recommendation}
\title{Render the recommendation slot separately}
\usage{
episode_duiding_recommendation(cluster, lang = "nl", instance_i18n = NULL)
}
\arguments{
\item{cluster}{A cluster object, see \code{episode_cluster_object()}.}

\item{lang}{Session language, \code{"nl"} or \code{"en"}.}

\item{instance_i18n}{Optional operator overrides, passed to \code{\link[=episode_tr]{episode_tr()}}.}
}
\value{
A single character string (the recommendation text), or \code{""} if
somehow nothing fired (should not happen: \code{recommendation.default}
always matches).
}
\description{
The recommendation slot is displayed with distinct visual treatment (an
advisory callout, not a plain paragraph) in the dossier, matching
\code{episode-mockup.jsx}'s \code{advies} box. This re-runs generation and returns
just that slot's text so the UI does not have to know fragment ids.
}
