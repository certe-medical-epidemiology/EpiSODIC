% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/triangle_update.R
\name{episode_triangle_completeness}
\alias{episode_triangle_completeness}
\title{Empirical completion curve for a stream}
\usage{
episode_triangle_completeness(con, stream_id, max_lag_days = 21)
}
\arguments{
\item{con}{A \link[DBI:DBIConnection-class]{DBI::DBIConnection}.}

\item{stream_id}{The stream to compute completeness for.}

\item{max_lag_days}{Maximum reporting lag (in days) to compute.}
}
\value{
A data frame with columns \code{lag_days} and \code{completeness}
(0-1, the median share of the final count visible at that lag).
}
\description{
The proportion of eventually-reported cases for a given sample date that
were already visible D days later, averaged across historical sample
dates. Used to shade the "incompleteness zone" on the epi curve (M2) and
to size the rolling detection window (ARCHITECTURE.md section 6, "the
window width taken from the triangle's empirical completion curve").
M1 computes it; M2 renders it.
}
