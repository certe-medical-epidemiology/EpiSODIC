% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geo_data.R
\name{episode_geo_overlay_resolve}
\alias{episode_geo_overlay_resolve}
\title{Resolve the optional region-outline overlay}
\usage{
episode_geo_overlay_resolve(
  path = Sys.getenv("EPISODE_GEO_DATA_OVERLAY", unset = NA)
)
}
\arguments{
\item{path}{Path to an \code{.rds} file holding an \code{sf} object with a
\code{geometry} column. Defaults to the \code{EPISODE_GEO_DATA_OVERLAY}
environment variable.}
}
\value{
An \code{sf} object, or \code{NULL} if \code{sf} is not installed, the
variable is unset, or the file is missing/invalid.
}
\description{
A second, independent geographic layer, drawn as outlines (no fill,
a thicker line) on top of the PC choropleth - for boundaries an
operator wants visible for orientation (provinces, municipalities,
catchment areas) but that carry no case counts of their own, so
\code{episode_geo_join()}'s \code{pc}-keyed contract does not apply here: the
overlay needs nothing but a \code{geometry} column. Unlike
\code{\link[=episode_geo_source_resolve]{episode_geo_source_resolve()}}, there is no shipped default -
region boundaries are far more jurisdiction-specific than postcode
geometry, and guessing at a "sensible default" (which country's
provinces?) would be arbitrary in a way the PC4 default is not
(\code{EPISODE_GEO_DATA} is Netherlands-only \emph{labelled as such}, not
pretending to be universal). No \code{EPISODE_GEO_DATA_OVERLAY} set (or an
invalid file) simply means no overlay layer, same as no \code{sf} at all.
}
\examples{
# NULL when unset (or when the sf package is not installed)
episode_geo_overlay_resolve(path = NA)
}
