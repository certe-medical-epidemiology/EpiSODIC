% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/notify.R
\name{episodic_setup_microsoft365}
\alias{episodic_setup_microsoft365}
\title{Set up Microsoft 365 authentication for notifications}
\usage{
episodic_setup_microsoft365(
  tenant_id,
  client_id = NULL,
  scopes = c("Mail.Send", "User.Read", "openid", "offline_access")
)
}
\arguments{
\item{tenant_id}{Your Azure AD tenant ID.}

\item{client_id}{Your Azure AD app registration's client ID. If
\code{NULL}, uses the Microsoft365R package default.}

\item{scopes}{Scopes to request. The default requests \code{Mail.Send}.}
}
\value{
Invisible \code{TRUE} on success.
}
\description{
Runs an interactive Azure AD login and caches the refresh token for
subsequent unattended use by \code{\link[=episodic_run_cron]{episodic_run_cron()}}. You only need to
run this once per server, unless the token expires. Requires the
\code{Microsoft365R} and \code{AzureGraph} packages.
}
\details{
If your Azure AD app registration has a client secret configured for
application-level (daemon) access, put the \code{client_secret} in the
instance YAML instead and skip this function entirely: the cron will
use the client-credentials flow directly.

If your staff already authenticate to Microsoft 365 through a shared
login on this server (for example by having called
\code{Microsoft365R::get_business_outlook()} interactively at some point, or
by running this function once), you can skip \code{client_id} entirely in
the instance YAML: \code{episodic_notify_microsoft365()} will reuse whatever
cached Azure AD token it finds for the configured \code{tenant_id} in
\code{AzureGraph::AzureR_dir()}, without any client ID, client secret, or new
interactive login.
}
\examples{
\dontrun{
episodic_setup_microsoft365(tenant_id = "your-tenant-id")
}
}
