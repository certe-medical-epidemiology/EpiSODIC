% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster_manual.R
\name{episodic_add_manual_cluster}
\alias{episodic_add_manual_cluster}
\title{Add One or More Manual (External) Clusters}
\usage{
episodic_add_manual_cluster(
  db_path = Sys.getenv("EPISODIC_DB", unset = NA),
  user_id,
  pathogen,
  level,
  first_day,
  last_day,
  n_cases = NA,
  care_line = NA,
  region_code = NA,
  institution_id = NA,
  ward = NA,
  expected = NA,
  excess = NA,
  ratio = NA,
  detector_agreement = 1L,
  priority_score = NA,
  case_dates = NULL,
  pc = NULL,
  sex = NULL,
  age = NULL,
  note = NA
)
}
\arguments{
\item{db_path}{Path to an existing SQLite database, or a \verb{mysql://}
DSN (see \code{\link[=episodic_db_dsn_mariadb]{episodic_db_dsn_mariadb()}}). Defaults to the
\code{EPISODIC_DB} environment variable.}

\item{user_id}{The \code{episodic_app_user} id this batch is attributed to
\itemize{
\item required, since provenance ("who added this, and when") is exactly
what a hand-added cluster most needs. Recorded as the author of the
seed note when \code{note} is supplied.
}}

\item{pathogen}{Raw pathogen string, exactly as it would appear in
\code{episodic_case$pathogen} - used verbatim, not matched against any
fixed list.}

\item{level}{One of the five lattice levels: \code{"pathogen_ward"},
\code{"pathogen_institution"}, \code{"pathogen_area"}, \code{"pathogen_province"},
\code{"pathogen_region"} (see \code{vignette("architecture")}).}

\item{first_day, last_day}{The cluster's episode window (\code{Date} or
\code{"YYYY-MM-DD"} text).}

\item{n_cases}{The case count. Optional if \code{case_dates} (or any other
per-cluster case-level detail) is supplied for that cluster, in
which case it defaults to that detail's own length; required
otherwise, since there is then nothing else to derive it from.}

\item{care_line, region_code, institution_id, ward}{The remaining
\code{episodic_stream} identity fields, exactly as
\code{episodic_stream_key()} expects them. \code{institution_id} must already
exist in \code{episodic_institution} (e.g. resolved earlier via
\code{episodic_institutions_resolve()}) - this function does not create
institutions.}

\item{expected, excess, ratio}{Optional pass-through metrics, for
external algorithms that do produce a baseline comparison. \code{NA}
(the default) when the source has no such concept - exactly as for
a detected cluster whose detector cannot produce one.}

\item{detector_agreement}{How many independent sources agree on this
cluster. Defaults to \code{1L} (a single external source).}

\item{priority_score}{Optional override. When \code{NA} (the default), it
is computed with the same \code{episodic_priority_score()} used for
detected clusters, so manual and detected clusters sort comparably -
from \code{expected}/\code{excess}/\code{ratio}, \code{detector_agreement}, and, when
per-cluster case-level detail is supplied, growth slope and spatial
concentration computed from it exactly as the cron pipeline would.}

\item{case_dates, pc, sex, age}{Optional per-cluster case-level detail,
each a \code{list()} of length \code{n} (one vector per cluster, all of equal
length within a cluster) - \code{case_dates} a vector of sample dates,
\code{pc} postcodes, \code{sex} \code{"M"}/\code{"F"}/\code{"U"}/\code{NA}, \code{age} in years. Feeds
the dossier's epi curve, demography and geography panels for this
cluster. Leave \code{NULL} (the default) for a source that only supplies
an aggregate count - the cluster is still created, its panels just
have nothing to draw.}

\item{note}{Optional markdown text to seed the cluster's note with
(see the dossier's notes panel), attributed to \code{user_id}.}
}
\value{
An integer vector of the newly created \code{cluster_id} values,
one per cluster, in the same order as the arguments.
}
\description{
Adds clusters that were never detected by EpiSODIC's own pipeline -
output from another algorithm or system, fed in as already-parsed R
values (an operator's own code is responsible for reading whatever
JSON/CSV that other system produces). A manual cluster is never
connected to this instance's own case data: it gets a real
\code{episodic_stream} identity (so it appears in the dashboard's normal
filtering, sorting and geography panels, exactly like a detected
cluster), but its case-level detail, if you supply any, is stored
separately in \code{episodic_cluster_manual_case} and never touches
\code{episodic_case}. It is therefore also never picked up by
\code{\link[=episodic_run_cron]{episodic_run_cron()}}'s reconciliation or automatic closure - a manual
cluster only closes when an epidemiologist records that verdict.
}
\details{
Every argument is vectorised: pass a single value to add one cluster,
or vectors/lists of length \code{n} to add \code{n} clusters in one call, each
getting its own new cluster id. Arguments of length 1 are recycled
against the longest argument you supply; per-cluster case-level detail
(\code{case_dates}/\code{pc}/\code{sex}/\code{age}) is never recycled - if you supply it,
it must be a \code{list()} of exactly the target length, one element (a
vector) per cluster.

The whole call is one transaction: either every requested cluster is
created, or (on any validation or database error) none are.
}
\examples{
\dontrun{
episodic_add_manual_cluster(
  user_id = 1L,
  pathogen = "Measles virus",
  level = "pathogen_area",
  first_day = "2025-01-10",
  last_day = "2025-01-20",
  region_code = "GR",
  case_dates = list(as.Date(c("2025-01-10", "2025-01-14", "2025-01-20"))),
  pc = list(c("9711AA", "9711AB", "9712CD")),
  note = "Reported by municipal health service, outside our own lab data."
)
}
}
