% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db_app_write.R
\name{db_app_write}
\alias{db_app_write}
\alias{episode_db_assessment_event_insert}
\alias{episode_db_stream_mute_insert}
\alias{episode_db_cluster_state_insert}
\alias{episode_db_report_render_insert}
\alias{episode_db_app_user_insert}
\title{App-side writers}
\usage{
episode_db_assessment_event_insert(
  con,
  cluster_id,
  user_id,
  verdict = NA,
  rationale,
  wpg_notifiable = NA,
  ggd_informed = NA,
  ggd_note = NA,
  snooze_until = NA,
  supersedes = NA
)

episode_db_stream_mute_insert(
  con,
  stream_id,
  muted_from,
  muted_until,
  reason,
  note = NA,
  user_id
)

episode_db_cluster_state_insert(
  con,
  cluster_id,
  state,
  trigger,
  event_id = NA,
  user_id = NA
)

episode_db_report_render_insert(
  con,
  cluster_id,
  user_id = NA,
  file_path,
  file_sha256,
  params_json,
  case_ids_json,
  version_no
)

episode_db_app_user_insert(
  con,
  username,
  full_name,
  email,
  password_hash,
  role = "assessor"
)
}
\arguments{
\item{con}{A \link[DBI:DBIConnection-class]{DBI::DBIConnection}.}
}
\description{
The app owns the judgements (ARCHITECTURE.md section 5.0) and only ever
inserts, never updates and never deletes (standing brief hard rule 7).
This is what makes the whole concurrency question disappear: two people
assessing the same cluster in the same minute produce two rows, both
visible in the timeline. Nothing in this file contains an \code{UPDATE} or a
\code{DELETE} statement; that absence is load-bearing and should be verified
by inspection, per MILESTONES.md M3's definition of done.
}
