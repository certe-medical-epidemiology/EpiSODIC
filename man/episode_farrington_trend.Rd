% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detect_farrington.R
\name{episode_farrington_trend}
\alias{episode_farrington_trend}
\title{Weekly Farrington trend points, for the multi-year trend panel}
\usage{
episode_farrington_trend(
  cases_for_stream,
  config,
  run_date = Sys.Date(),
  n_weeks_existing = 0L,
  max_backfill_weeks = 156L,
  population = NULL
)
}
\arguments{
\item{cases_for_stream}{A data frame of a single stream's cases.}

\item{config}{The resolved configuration; uses \code{config$farrington}.}

\item{run_date}{The date to treat as "today".}

\item{n_weeks_existing}{How many trend weeks are already persisted for
this stream (from \code{episode_db_stream_trend()}); determines how much
backfill is attempted.}

\item{max_backfill_weeks}{Cap on how many weeks a single call will ever
(re-)compute, to bound cron run time. Matches the multi-year trend
panel's own display window.}

\item{population}{See \code{\link[=episode_detect_farrington]{episode_detect_farrington()}}.}
}
\value{
A data frame with \code{week_start}, \code{n_cases}, \code{expected},
\code{upperbound} (zero rows if ineligible).
}
\description{
The app performs cheap reads only (ARCHITECTURE.md section 3.3) and
cannot re-run \code{farringtonFlexible()} at render time, so the cron must
persist a continuous expected/upperbound series rather than only the
current week's alarm status (see \code{episode_detect_farrington()} and
\code{QUESTIONS.md}). This function returns that series; the caller
(\code{episode_run_cron()}) is responsible for upserting it into
\code{episode_stream_trend}.
}
\details{
Only the trailing \code{n_weeks_to_compute} weeks are (re-)evaluated per call,
not the whole history: on a normal nightly run that is 1 (today's week
alone), which is cheap. A fresh stream with no trend rows yet gets a
larger backfill (bounded by \code{max_backfill_weeks}) so the panel is not
empty on first render, at the cost of a heavier one-off computation.
}
