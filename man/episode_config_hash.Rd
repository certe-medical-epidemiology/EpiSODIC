% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{episode_config_hash}
\alias{episode_config_hash}
\title{Compute the SHA-1 hash and canonical JSON snapshot of a resolved config}
\usage{
episode_config_hash(config)
}
\arguments{
\item{config}{A resolved configuration, as returned by
\code{\link[=episode_config_resolve]{episode_config_resolve()}}.}
}
\value{
A list with elements \code{hash} (a 40-character SHA-1 hex digest) and
\code{snapshot} (the canonical JSON string).
}
\description{
The hash is taken over a canonicalised representation (keys sorted
recursively, then serialised as JSON) so that key ordering in the source
YAML never changes the hash. See \code{QUESTIONS.md} item 15: the hashing
algorithm itself is not specified by the architecture and was chosen to
match the \code{CHAR(40)} width used for other \verb{_key}/\verb{_hash} columns in the
schema.
}
\examples{
hashed <- episode_config_hash(episode_config_resolve())
hashed$hash
}
