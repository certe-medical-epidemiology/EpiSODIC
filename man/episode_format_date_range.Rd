% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/i18n.R
\name{episode_format_date_range}
\alias{episode_format_date_range}
\title{Format a date range compactly, collapsing shared month/year}
\usage{
episode_format_date_range(x, y, lang = "nl")
}
\arguments{
\item{x, y}{Range endpoints - \code{Date}, or a string \code{as.Date()} accepts.
Order does not matter; the earlier date is always shown first.}

\item{lang}{Session language, \code{"nl"} (default) or \code{"en"}.}
}
\value{
A character string, or \code{episode_tr("misc.unknown", lang = lang)} if either endpoint fails to parse.
}
\description{
\code{"7-15 jan. 2025"} rather than \code{"2025-01-07 to 2025-01-15"}: shared
month and year are stated once, not repeated per endpoint. Falls back
one step at a time as the range widens (same month -> same year ->
different years), and collapses to a single date when \code{x} and \code{y} are
the same day (a one-day cluster should not read "7-7 jan. 2025").
}
