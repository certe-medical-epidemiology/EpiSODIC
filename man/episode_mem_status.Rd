% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detect_mem.R
\name{episode_mem_status}
\alias{episode_mem_status}
\title{Compute this stream's current MEM status}
\usage{
episode_mem_status(cases, run_date = Sys.Date(), min_seasons = 2L)
}
\arguments{
\item{cases}{A data frame with \code{sample_date}, all of a stream's known
cases (not just one cluster's).}

\item{run_date}{The date to treat as "today".}

\item{min_seasons}{Minimum number of complete prior seasons required.}
}
\value{
\code{NULL} if \code{mem} is not installed, there is no case data, the
current date falls outside the surveillance season window, or fewer
than \code{min_seasons} complete prior seasons exist. Otherwise a list:
\code{epidemic_started} (logical), \code{current_week_count},
\code{pre_epidemic_threshold}, \code{post_epidemic_threshold}, \code{week_start},
\code{week_end} (the current epi week's \code{Date} bounds).
}
\description{
Shared by \code{\link[=episode_detect_mem]{episode_detect_mem()}} (fires on \code{epidemic_started}) and the
seasonal closure criterion (fires when the current count has fallen
back under \code{post_epidemic_threshold} - ARCHITECTURE.md sections 6.3 and
7.3), so both read the same fitted model rather than risking two
slightly different ones.
}
