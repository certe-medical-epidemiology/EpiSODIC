% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db_read.R
\name{db_read}
\alias{db_read}
\alias{episode_db_pathogen_config}
\alias{episode_db_pathogen_config_get}
\alias{episode_db_streams}
\alias{episode_db_stream_get}
\alias{episode_db_institutions}
\alias{episode_db_institution_get}
\alias{episode_db_cases}
\alias{episode_db_cases_for_mo}
\alias{episode_db_clusters}
\alias{episode_db_clusters_for_stream}
\alias{episode_db_cluster_cases}
\alias{episode_db_assessment_events}
\alias{episode_db_detections_for_run}
\alias{episode_db_latest_run}
\title{Shared read helpers}
\usage{
episode_db_pathogen_config(con)

episode_db_pathogen_config_get(con, mo_code)

episode_db_streams(con, active_only = TRUE)

episode_db_stream_get(con, stream_key)

episode_db_institutions(con)

episode_db_institution_get(con, institution_key)

episode_db_cases(con)

episode_db_cases_for_mo(con, mo_code)

episode_db_clusters(con, open_only = FALSE)

episode_db_clusters_for_stream(con, stream_id)

episode_db_cluster_cases(con, cluster_id)

episode_db_assessment_events(con, cluster_id)

episode_db_detections_for_run(con, run_id)

episode_db_latest_run(con, status = NULL)
}
\arguments{
\item{con}{A \link[DBI:DBIConnection-class]{DBI::DBIConnection}.}

\item{mo_code}{A single pathogen code.}

\item{active_only}{If \code{TRUE} (default), only streams with \code{is_active = 1}.}

\item{stream_key}{A single \code{stream_key}.}

\item{institution_key}{A single \code{institution_key}.}

\item{open_only}{If \code{TRUE}, exclude clusters with \code{merged_into} set.}

\item{stream_id}{A single \code{stream_id}.}

\item{cluster_id}{A single \code{cluster_id}.}

\item{run_id}{A single \code{run_id}.}

\item{status}{If given, only the latest run with this \code{status}.}
}
\description{
Plain \code{DBI}-based readers used by both the cron and the app. Read-only:
none of these functions write to the database. All SQL for the package
lives behind functions in \code{R/db_*.R}; nothing outside this layer issues
SQL directly (standing brief, section 3).
}
