% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analytics_similar_clusters.R
\name{episode_app_similar_clusters}
\alias{episode_app_similar_clusters}
\title{Find the most similar closed clusters ("Vergelijkbare clusters")}
\usage{
episode_app_similar_clusters(con, cluster_id, lang = "nl", n = 3L)
}
\arguments{
\item{con}{A \link[DBI:DBIConnection-class]{DBI::DBIConnection}.}

\item{cluster_id}{The cluster to find precedents for (excluded from its
own results).}

\item{lang}{Session language.}

\item{n}{Maximum number of results.}
}
\value{
A data frame, one row per similar cluster, ordered by
similarity descending: \code{cluster_id}, \code{pathogen}, \code{level_label},
\code{place}, \code{n_cases}, \code{verdict_label}, \code{closed_at}. Zero rows if there
is no closed precedent for this pathogen.
}
\description{
A panel on the dossier surfacing the three most similar assessed
clusters from the archive, matched on organism, level, size, season
and duration, each showing what was decided and why (ARCHITECTURE.md
section 10.1). "At roughly ten clusters a month there will be two
hundred assessed precedents within two years, which makes the archive
a decision aid rather than a filing cabinet."
}
\details{
Matching requires the same pathogen (exact string match, as everywhere
else in this codebase - \code{QUESTIONS.md} item 22) and only ever considers
\emph{closed} clusters (an open one is not yet a precedent). Similarity
within that pool is a simple, documented scoring rule rather than a
fitted model, since ARCHITECTURE.md specifies the matching dimensions
but not a weighting scheme - a judgement call flagged in QUESTIONS.md
alongside the codebase's other provisional numeric choices:
\itemize{
\item Level: +1 if the same lattice level, else 0.
\item Size: \verb{1 / (1 + |log((n1+1)/(n2+1))|)} - a smooth similarity that
treats a doubling and a halving of case count symmetrically.
\item Season: circular day-of-year distance between the two clusters'
\code{first_day}, so a cluster starting in late December and one in early
January score as seasonally close, not far apart.
\item Duration: \verb{1 / (1 + |dur1 - dur2| / 7)}, in days.
}

Each dimension contributes in \verb{[0, 1]}; the total is their unweighted
sum, ranking candidates within the same-pathogen pool.
}
