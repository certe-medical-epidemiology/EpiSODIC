% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detect_certestats.R
\name{detect_certestats}
\alias{detect_certestats}
\alias{episode_detect_clusters}
\alias{episode_detect_farrington}
\title{Wrappers around \code{certestats} detectors}
\usage{
episode_detect_clusters(con, cases_for_stream, stream_id)

episode_detect_farrington(
  con,
  cases_for_stream,
  stream_id,
  population_offset = NULL
)
}
\arguments{
\item{con}{A \link[DBI:DBIConnection-class]{DBI::DBIConnection}.}

\item{cases_for_stream}{A data frame of a single stream's cases, with
\code{sample_date}.}

\item{stream_id}{The stream these cases belong to.}
}
\value{
A data frame of detection records (possibly zero rows if
\code{certestats} is unavailable or found nothing).
}
\description{
\code{certestats::detect_disease_clusters()} (detector \code{'clusters'}) and
\code{certestats::detect_farrington()} (detector \code{'farrington'}) are the
constraint-fixed detection functions (ARCHITECTURE.md section 2).
\code{certestats} is Certe-internal and cannot be installed or exercised in
this environment (standing brief section 2), so these wrappers are
guarded with \code{requireNamespace()} throughout and, when the package is
unavailable, return zero detections rather than failing the run - the
documented fallback path a stranger cloning the repository needs.
}
\details{
Golden-file tests around these wrappers belong in
\code{tests/testthat/test-detect_certestats.R} once \code{certestats} is available
to generate the golden output against; see \code{QUESTIONS.md} items 10 and 16
and the standing brief section 6.
}
